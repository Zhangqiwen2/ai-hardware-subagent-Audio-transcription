# AgentArts 智能体镜像构建与部署指导手册

> 本手册以录音文件转写 Agent 为例，完整讲解从零开始在 AgentArts 平台上构建 Docker 镜像并部署为智能体运行时的全流程。适用于任何 Python 类智能体。

---

## 一、整体流程概览

```
开发机(Mac/PC)
    │ ① 上传代码到构建机
    ▼
构建机(鲲鹏 ECS, ARM64 Linux)
    │ ② agentarts launch 自动执行：
    │    a. 构建 ARM64 Docker 镜像
    │    b. 推送镜像到 SWR
    │    c. 创建/更新 AgentArts 运行时
    ▼
AgentArts 平台（云端）
    │ ③ 控制台配置：环境变量、出站网络
    ▼
智能体运行时（对外提供 HTTP 服务）
    │ ④ 调用方通过 /invocations 接口调用
    ▼
```

---

## 二、前置条件

### 2.1 所需账号与权限

| 权限 | 说明 | 获取方式 |
|---|---|---|
| **华为云主账号**（或 IAM 子账号） | 必须有 AgentArts + SWR 权限 | 华为云注册 |
| **AK/SK**（访问密钥） | 用于 `agentarts` CLI 认证，推送 SWR | 控制台 → 右上角头像 → 「我的凭证」→「访问密钥」→ 新增（CSV 仅能下载一次，妥善保存） |
| **AgentArts 使用权限** | 部署智能体运行时 | 需要平台开通（目前仅西南-贵阳一区域） |
| **SWR 推送权限** | 推送 Docker 镜像 | 通常主账号自带；IAM 子账号需授权 |

### 2.2 所需资源

| 资源 | 规格 | 说明 |
|---|---|---|
| **鲲鹏 ECS**（ARM64 Linux） | Ubuntu 22.04, 2vCPU/4GB 起 | 构建镜像用。**必须是 ARM64**，X86 构建的镜像部署后调用会失败 |
| **Python 3.10+** | ECS 上安装 | agentarts-sdk 要求 |
| **Docker 18.06+** | ECS 上安装 | 构建镜像用 |
| **公网 IP** | ECS 必须有 | SSH、拉基础镜像、推 SWR 都需要 |

### 2.3 项目代码要求

项目必须包含以下文件：

| 文件 | 作用 | 示例 |
|---|---|---|
| `app.py` | Agent 入口，使用 `agentarts-sdk` 的 `AgentArtsRuntimeApp` | 见下方代码模板 |
| `requirements.txt` | Python 依赖清单，**必须包含 `agentarts-sdk`** | `agentarts-sdk\nrequests\npython-dotenv` |
| `.env` / 环境变量 | 业务密钥（如讯飞 API Key） | 部署后在控制台配置 |

**`app.py` 最小模板：**

```python
from agentarts.sdk import AgentArtsRuntimeApp, RequestContext

app = AgentArtsRuntimeApp()

@app.entrypoint
def handler(payload: dict, context: RequestContext = None) -> dict:
    """Agent 入口：处理 /invocations 请求"""
    # 业务逻辑写在这里
    return {"output": {"text": "处理结果"}}

if __name__ == "__main__":
    import os
    port = int(os.getenv("AGENT_RUN_PORT", "8080"))
    app.run(port=port)
```

---

## 三、环境搭建（首次）

### 3.1 购买鲲鹏 ECS

华为云控制台 → ECS → 购买实例：

| 配置项 | 填写值 | 注意 |
|---|---|---|
| 区域 | **西南-贵阳一（cn-southwest-2）** | 必须与 AgentArts 同区域 |
| 架构 | **鲲鹏 ARM64** | ⚠️ 关键！X86 会导致部署后调用失败 |
| 镜像 | Ubuntu 22.04 | |
| 规格 | 2vCPU / 4GB 起步 | 构建够用 |
| 公网 IP | 必须购买 | |
| 安全组 | 放通 22 端口 | SSH 用 |

### 3.2 初始化 ECS 环境

SSH 登录 ECS 后，安装基础环境：

```bash
# ① 安装 Python + venv
sudo apt update
sudo apt install -y python3 python3-venv python3-pip

# ② 安装 Docker
sudo apt install -y docker.io
sudo systemctl enable --now docker
sudo usermod -aG docker $USER  # 免 sudo
# ⚠️ 需退出重新登录生效

# ③ 配置 Docker 镜像加速（避免拉 python:3.10-slim 超时）
sudo tee /etc/docker/daemon.json << 'EOF'
{"registry-mirrors": ["https://docker.1ms.run", "https://docker.1panel.live"]}
EOF
sudo systemctl daemon-reload && sudo systemctl restart docker

# ④ 创建虚拟环境并安装 agentarts-sdk
python3 -m venv ~/venv
source ~/venv/bin/activate
pip install agentarts-sdk
```

### 3.3 配置华为云凭证

```bash
# 方式A：临时设置（当前会话有效）
export HUAWEICLOUD_SDK_AK='你的AK'
export HUAWEICLOUD_SDK_SK='你的SK'

# 方式B：持久化到 ~/.bashrc（推荐）
echo 'export HUAWEICLOUD_SDK_AK="你的AK"' >> ~/.bashrc
echo 'export HUAWEICLOUD_SDK_SK="你的SK"' >> ~/.bashrc
source ~/.bashrc
```

> ⚠️ 注意用**直引号** `"`，不要用弯引号 `"`，否则 AK/SK 值中会混入特殊字符导致认证失败。

**验证 AK/SK 是否配置正确：**

```bash
python3 -c "import os; print('AK:', os.environ.get('HUAWEICLOUD_SDK_AK','').isascii())"
# 应输出 AK: True
```

---

## 四、配置智能体（首次）

```bash
cd ~/你的项目目录
source ~/venv/bin/activate

agentarts configure --entrypoint app:app
```

交互式填写以下参数：

| 参数 | 填写值 | 说明 |
|---|---|---|
| 智能体名称 | `my-agent`（小写字母开头，可含数字、中划线） | 运行时名称，调用 URL 中会用到 |
| 部署区域 | `cn-southwest-2` | 目前仅支持西南-贵阳一 |
| 依赖文件 | `requirements.txt` | 必须包含 `agentarts-sdk` |
| SWR 镜像组织名 | 用默认（自动生成） | 若自定义，需先在 SWR 控制台创建同名组织 |

配置完成后会生成 `.agentarts_config.yaml` 文件（可手动编辑调整认证方式等）。

### 认证方式配置（可选）

默认使用 **IAM 认证**（V11 签名）。如需改为 **API Key 认证**（调用方只需 Bearer token），编辑 `.agentarts_config.yaml`：

```yaml
identity_configuration:
  authorizer_type: API_KEY
  authorizer_configuration:
    key_auth:
      api_keys:
        - api_key: your-api-key-value     # 自定义 API Key
          api_key_name: my-key
```

> ⚠️ 认证方式在运行时创建后**不能修改**，改配置后需删除旧运行时重新部署。

---

## 五、构建镜像并部署

```bash
source ~/venv/bin/activate
export DOCKER_BUILDKIT=0   # Docker 27+ 需要，避免 OCI 格式不兼容

agentarts launch
```

### 5.1 `agentarts launch` 自动执行的 5 步

```
Step 1/5 Building Docker image      ← 构建 ARM64 镜像（自动生成 Dockerfile）
Step 2/5 Setting up SWR resources   ← 创建/复用 SWR 组织和仓库
Step 3/5 Getting SWR credentials    ← 自动登录 SWR
Step 4/5 Tagging and pushing image  ← 推送镜像到 SWR
Step 5/5 Creating AgentArts runtime ← 创建/更新云端运行时
```

### 5.2 成功输出示例

```
√ SUCCESS: Runtime 'my-agent(v1)' created successfully
╭─────────────── Deployment complete! ───────────────╮
│ Agent Name:      my-agent                          │
│ Runtime ID:      xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxx  │
│ Image:           swr.cn-southwest-2.myhuaweicloud.com/组织名/my-agent:latest
│ Region:          cn-southwest-2                    │
│ Access Endpoint: defaultgw-xxx.cn-southwest-2.huaweicloud-agentarts.com
╰────────────────────────────────────────────────────╯
```

**记录以下信息（调用方需要）：**
- **Runtime ID**：运行时唯一标识
- **Access Endpoint**：访问域名（HTTP 调用的网关地址）
- **API Key**（如配置了 API_KEY 认证）

---

## 六、控制台配置运行时（部署后必做）

登录 AgentArts 控制台 → 部署运行 → 智能体运行时 → 点击你的运行时：

### 6.1 环境变量

填入业务所需的密钥（如讯飞 API Key）：

```
IFLYTEK_APP_ID=xxx
IFLYTEK_API_KEY=xxx
IFLYTEK_API_SECRET=xxx
```

### 6.2 出站网络

选 **`PUBLIC`**（如果 Agent 需要访问外部 API）。

### 6.3 修改环境变量后需重新部署

在 ECS 上重新执行 `agentarts launch` 使环境变量生效。

---

## 七、调用智能体

### 7.1 请求地址

```http
POST https://{Access Endpoint}/runtimes/{Agent Name}/invocations
```

### 7.2 请求头

| Header | 值 |
|---|---|
| Content-Type | application/json |
| Authorization | Bearer {API Key}（或 IAM V11 签名） |
| X-Hw-Agentarts-Session-Id | 任意字符串（如 "sess-001"） |

### 7.3 请求体（统一 inputs 格式）

```json
{
  "inputs": {
    "operation": "chat_completions",
    "file_url": ["https://obs.../file.wav"]
  }
}
```

### 7.4 curl 示例

```bash
curl -s -X POST https://{域名}/runtimes/{名称}/invocations \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer {API_KEY}" \
  -H "X-Hw-Agentarts-Session-Id: sess-001" \
  -d '{"inputs":{"operation":"query_capabilities"}}'
```

---

## 八、后续更新代码

代码修改后，在**构建机**上重新执行：

```bash
cd ~/你的项目目录
source ~/venv/bin/activate
export DOCKER_BUILDKIT=0
agentarts launch
```

镜像层有缓存，只会重新构建变化的部分，速度很快。

> ⚠️ 注意：`rsync --delete` 等同步工具可能误删 ECS 上生成的 `.agentarts_config.yaml` 和 `Dockerfile`。如文件被删，重新执行 `agentarts configure --entrypoint app:app` 再 `agentarts launch`。

---

## 九、常见问题

| 现象 | 原因/解决 |
|---|---|
| AK/SK 认证失败 | 环境变量未生效；重新 `export` 或检查 `~/.bashrc` |
| 拉基础镜像超时 | Docker 镜像加速未配置；参考 3.2 步骤③ |
| 部署后调用返回 502 | 镜像非 ARM64（用 X86 机器构建了） |
| OCI 格式错误 | Docker 27+；执行 `export DOCKER_BUILDKIT=0` |
| 环境变量不生效 | 控制台配完后需重新 `agentarts launch` 部署 |
| 认证方式改不了 | 运行时创建后认证方式不可改；删除旧运行时重建 |
| `File transfer config cannot be modified` | 同上，文件传输配置也不可改 |
| SWR 组织不存在 | 自定义组织名需先在 SWR 控制台（贵阳一）创建 |
| 403 Insufficient permission | IAM 子账号未授权 SWR |

---

## 十、关键参数速查表

| 参数 | 值 | 来源 |
|---|---|---|
| 区域 | cn-southwest-2 | AgentArts 目前唯一支持区域 |
| 架构 | ARM64（鲲鹏） | 必须匹配 |
| 基础镜像 | python:3.10-slim | agentarts-sdk 自动使用 |
| Agent 端口 | 8080 | SDK 默认，通过 `AGENT_RUN_PORT` 环境变量 |
| 入口文件 | app.py | `--entrypoint app:app` 指定 |
| 依赖文件 | requirements.txt | `configure` 时指定 |
| 认证方式 | IAM（默认）或 API_KEY | `.agentarts_config.yaml` 中配置 |
| HTTP 路径 | /runtimes/{名称}/invocations | AgentArts 平台固定规范 |
| 必带请求头 | X-Hw-Agentarts-Session-Id | AgentArts 平台要求 |

---

## 附录：文件清单

部署一个 Agent 最少需要这些文件：

```
my-agent/
├── app.py              # 入口（AgentArtsRuntimeApp + @app.entrypoint）
├── requirements.txt    # 依赖（必须含 agentarts-sdk）
├── .env                # 本地开发密钥（部署后在控制台配环境变量）
└── .env.example        # 密钥模板（可提交到 git）
```

Dockerfile 由 `agentarts launch` **自动生成**，不需要手写。
