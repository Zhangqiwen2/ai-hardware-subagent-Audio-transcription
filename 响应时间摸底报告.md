# 录音文件转写 Agent - 响应时间摸底报告

> 测试日期：2026-08-22 ｜ 环境：AgentArts 云端运行时（v8，API Key 认证）
> Runtime ID：c94a22a5-6b57-4be4-beff-574a7b1ef9a5
> 访问域名：defaultgw-grstqnldg5.cn-southwest-2.huaweicloud-agentarts.com

---

## 一、测试目的

为联调提供数据支撑，明确不同时长音频的转写耗时，帮助调用方：
- 设置合理的请求超时
- 决定走同步（chat_completions）还是异步（create_response + fetch_response）
- 评估用户体验

---

## 二、测试方法

### 2.1 样本

以现有 98 秒新闻音频为源，通过循环拼接生成 4 个不同时长的测试样本：

| 样本 | 音频时长 | 文件大小 | 来源 |
|---|---|---|---|
| 30秒 | 30s | 938 KB | 截取前 30 秒 |
| 2分钟 | 120s | 3.7 MB | 循环 1.2x |
| 5分钟 | 300s | 9.4 MB | 循环 3.1x |
| 10分钟 | 600s | 18.8 MB | 循环 6.1x |

> ⚠️ 注意：长音频由同一段内容循环拼接，讯飞可能对重复内容有优化，真实场景（不同说话人、不同内容）可能略有偏差。

### 2.2 测试流程

每个样本跑 2 轮取平均：

1. **同步测试**（`chat_completions`）：单次请求，阻塞直到转写完成，记录 wall-clock
2. **异步测试**（`create_response` + `fetch_response`）：创建任务后每 10 秒轮询一次，直到 completed

### 2.3 耗时拆解

从 v8 版本起，Agent 支持结构化耗时日志，每次转写会输出三段耗时：

```
耗时分解: total=17000ms | agent=500ms | iflytek=16500ms (upload=1200ms + process=15300ms)
```

| 字段 | 含义 | 责任方 |
|---|---|---|
| `agent` | 下载音频 + 解析 payload + 格式转换 | Agent |
| `iflytek.upload` | 上传音频到讯飞并被接收 | 讯飞 API |
| `iflytek.process` | 讯飞转写处理 + 轮询等待 | 讯飞 API |

---

## 三、实测数据

### 3.1 原始耗时

| 样本 | 音频时长 | 同步 avg | 同步 min | 同步 max | 异步 avg | 异步 min | 异步 max |
|---|---|---|---|---|---|---|---|
| 30秒 | 30s | 13.4s | 11.2s | 15.6s | 20.5s | 20.5s | 20.5s |
| 2分钟 | 120s | 11.3s | 11.2s | 11.3s | 20.6s | 20.5s | 20.6s |
| 5分钟 | 300s | 16.9s | 11.8s | 22.1s | 20.5s | 20.5s | 20.6s |
| 10分钟 | 600s | 22.7s | 22.6s | 22.8s | 30.7s | 30.7s | 30.8s |

### 3.2 耗时比（耗时 / 音频时长）

| 样本 | 音频时长 | 同步/时长 | 异步/时长 | 结论 |
|---|---|---|---|---|
| 30秒 | 30s | 0.45x | 0.68x | 快 |
| 2分钟 | 120s | 0.09x | 0.17x | 快 |
| 5分钟 | 300s | 0.06x | 0.07x | 快 |
| 10分钟 | 600s | 0.04x | 0.05x | 快 |

### 3.3 关键发现

1. **转写速度远快于实时**：综合耗时比约 0.16x，即讯飞处理速度约为音频时长的 **6 倍**
2. **耗时与音频时长弱相关**：30 秒和 10 分钟的同步耗时仅差约 2 倍，固定开销（上传 + 排队）占主导
3. **异步轮询是额外开销**：异步总耗时 ≈ `poll_interval × poll_count`，实际转写可能在第一次轮询前已完成
4. **同步 vs 异步差异不大**：因为讯飞快，同步阻塞时间本身已很短；异步的额外开销主要来自轮询间隔

---

## 四、估算参考

基于实测平均耗时比 **0.16x** 推算：

| 音频时长 | 预估转写耗时（同步） | 建议调用方式 |
|---|---|---|
| 1 分钟 | ~10 秒 | 同步 |
| 5 分钟 | ~48 秒 | 同步 |
| 10 分钟 | ~1.5 分钟 | 同步 / 异步 |
| 30 分钟 | ~5 分钟 | 异步 |
| 1 小时 | ~10 分钟 | 异步 |
| 3 小时（上限） | ~29 分钟 | 异步 |

> ⚠️ 以上为粗略估算，实际受音频格式、网络状况、讯飞服务负载影响。

---

## 五、调用建议

| 场景 | 推荐方式 | 超时设置 | 说明 |
|---|---|---|---|
| 短音频（< 5 分钟） | 同步 `chat_completions` | 120 秒 | 简单，一次拿到结果 |
| 长音频（> 5 分钟） | 异步 `create_response` + `fetch_response` | 创建 30 秒 | 避免长连接超时 |
| 超长音频（> 30 分钟） | 异步 + 落库 | - | 防止运行时重启丢任务 |

---

## 六、后续优化方向

1. **更精细摸底**：使用不同内容（非循环拼接）的音频测试，验证重复内容是否被讯飞优化
2. **生产监控**：接入耗时日志统计（P50/P95/P99），持续观测转写性能
3. **轮询策略优化**：异步轮询间隔可动态调整（如指数退避），减少 API 调用次数

---

## 附录：原始数据

完整的逐轮原始数据已保存为 `benchmark_20260822_110423.json`，包含每次请求的精确耗时。
