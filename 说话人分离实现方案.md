# 说话人分离 + 时间戳 实现方案

> 目标：转写结果支持区分说话人（speakerId）和逐段时间戳（beginTimeMs/endTimeMs），
> 同时保持现有纯文本接口完全向后兼容。
> 日期：2026-09-01

---

## 一、能力依据（讯飞 API）

**上传接口**增加两个可选参数：

| 参数 | 取值 | 说明 |
|------|------|------|
| `roleType` | `0` / `1` / `3` | 0-不开启（现状默认），**1-通用角色分离（本方案用）**，3-声纹分离（需注册声纹） |
| `roleNum` | `[0, 10]` | 说话人数，**0=盲分**（讯飞自动判断人数），最大 10 |

**结果**中每个 VAD 分段（`st` 对象）返回：

| 讯飞字段 | 含义 | 映射到目标 |
|----------|------|-----------|
| `st.bg` | 分段开始时间（ms，字符串） | `beginTimeMs` |
| `st.ed` | 分段结束时间（ms，字符串） | `endTimeMs` |
| `st.rl` | 角色编号（**仅开启分离时返回**，字符串数字） | `speakerId` |
| `st.rt[].ws[].cw[].w` | 分词文本 | `text`（按分段拼接） |

---

## 二、对外接口变更

### 2.1 请求格式（新增可选字段 `enable_speaker`）

```json
{
  "inputs": {
    "operation": "chat_completions",
    "file_url": ["https://obs.../audio.wav"],
    "enable_speaker": true
  }
}
```

- `enable_speaker`：bool，**可选，默认 false**
- `false` 或不传：行为与现在**完全一致**（零风险上线）
- `true`：内部映射 `roleType=1, roleNum=0`（通用盲分），返回 sentences
- `create_response` 同样支持该字段

### 2.2 同步响应（SSE responseContent）

**不开启（现状，不变）：**
```json
{"message": "我们下周确认方案。好的，我来记录下来。"}
```

**开启 enable_speaker：**
```json
{
  "message": "我们下周确认方案。好的，我来记录下来。",
  "sentences": [
    {"text": "我们下周确认方案。", "speakerId": 1, "beginTimeMs": 4220, "endTimeMs": 6380},
    {"text": "好的，我来记录下来。", "speakerId": 2, "beginTimeMs": 6710, "endTimeMs": 9210}
  ]
}
```

- `speakerId`：int。未开启分离/该段无 `rl` 时为 `null`
- 时间戳：int，毫秒，相对音频开头
- `message` 字段不变，主 Agent 老解析逻辑不受影响

### 2.3 异步响应（fetch_response completed，顶层新增 sentences）

```json
{
  "id": "resp_xxx",
  "object": "response",
  "status": "completed",
  "output": [{"content": [{"type": "output_text", "text": "完整文本..."}]}],
  "sentences": [
    {"text": "我们下周确认方案。", "speakerId": 1, "beginTimeMs": 4220, "endTimeMs": 6380}
  ]
}
```

- 仅开启 enable_speaker 的任务才返回 `sentences`（顶层字段，与 error_code 同级风格）
- 不开启时响应与现在完全一致

### 2.4 不支持的字段（说明）

- **逐句 language**：讯飞只有文件级语种（`analysis` 开启后 `orderInfo.language`），
  无逐句语种，目标格式中的 `language` 字段**不提供**

---

## 三、内部实现设计

### 3.1 调用链路（改动点标注 *）

```
operation_router.handle_invocation
  * _extract_request: 提取 enable_speaker -> request["enable_speaker"]
  * chat_completions: transcribe_fn 返回值兼容 str/dict；responseContent 加 sentences
  * create_response: request 带 enable_speaker（随 payload 进任务）
  * fetch_response: completed 响应加 task["sentences"]（顶层）
        │
        ▼
service.transcribe_from_payload
  * 提取 enable_speaker -> role_type/role_num
  * 开启时返回 dict {"text", "sentences"}；不开启返回 str（现状）
        │
        ├── 直调讯飞：XfyunAsrClient
  *     upload_audio 加 role_type/role_num 参数（url_params 加 roleType/roleNum）
  *     transcribe 透传；内部解析 sentences
        │
        └── 网关：GatewayAsrClient
  *     upload_audio 的 options 加 roleType/roleNum
  *     transcribe 透传；解析复用 result_parser
        │
        ▼
* result_parser: 新增 parse_sentences(api_response) -> list[dict]
  遍历 lattice -> 每个 st 提取 bg/ed/rl + 拼接分词文本
```

### 3.2 result_parser 新增解析函数

```python
def parse_sentences(api_response: dict) -> list[dict]:
    """提取分段级结构化结果：text + speakerId + beginTimeMs + endTimeMs。

    遍历 lattice（每个元素 = 一个 VAD 分段）：
    - bg/ed: 字符串毫秒 -> int
    - rl:    字符串数字 -> int；缺失 -> None
    - text:  拼接 rt[].ws[].cw[].w（含标点，与纯文本逻辑一致）
    """
```

返回：
```python
[{"text": str, "speakerId": int|None, "beginTimeMs": int, "endTimeMs": int}, ...]
```

### 3.3 service 返回值约定（关键兼容设计）

`transcribe_from_payload` 返回类型：

| 场景 | 返回 | 说明 |
|------|------|------|
| 未开启 enable_speaker | `str` | 与现状完全一致 |
| 开启 enable_speaker | `dict{"text": str, "sentences": list}` | 新增结构 |

消费端统一做 `isinstance(result, dict)` 分支处理：

```python
# operation_router / async_tasks 通用兼容写法
if isinstance(result, dict):
    text, sentences = result.get("text", ""), result.get("sentences")
else:
    text, sentences = result, None
```

好处：现有单测的 mock（返回 str）零改动，生产链路按 dict 处理。

### 3.4 async_tasks 存储扩展

`_run` 中任务完成时新增：

```python
self._tasks[response_id]["sentences"] = sentences   # 可能为 None
```

task dict 增加一个可选 key，过期/清理逻辑不受影响。

### 3.5 参数传递细节

- **直调讯飞**：`url_params` 中 `roleType`/`roleNum` **仅在开启时传**（减少签名变量，
  避免未开启时多余参数）
- **网关**：`options` 中加 `"roleType": 1, "roleNum": 0`（仅在开启时加）
- `enable_speaker` 从 `inputs` 提取（`_extract_request`），与 `model_config` 提取方式一致

---

## 四、兼容性评估

| 场景 | 影响 |
|------|------|
| 老调用（不传 enable_speaker） | **零影响**：请求、讯飞参数、响应格式全部不变 |
| 新调用（enable_speaker=true） | 响应多 sentences 字段；讯飞侧多 roleType/roleNum 参数 |
| 单测现有 14 个用例 | 零改动（mock 返回 str，走 isinstance 兼容分支） |
| 网关路径（model_config 注入） | 需确认网关透传 roleType/roleNum（见风险①） |

---

## 五、风险与前置验证

| # | 风险 | 验证/缓解 |
|---|------|----------|
| ① | **讯飞账号可能未开通角色分离权限**（类似之前 autodialect 报 100020） | 上线前先直调验证：传 roleType=1 转一个测试音频，看是否报错 |
| ② | **ConvAIAgent 网关可能不透传 roleType/roleNum** | 先走直调路径验证能力；网关路径联调时确认，不透传则需网关侧改造（二期） |
| ③ | roleNum 传非法值报 100033 | 固定盲分 roleNum=0，不暴露给调用方 |
| ④ | VAD 分段 ≠ 完整句子（一段可能多句） | 一期按分段粒度返回；如需句号级切分，二期按标点（wp=p）再切 |
| ⑤ | 分离错误（把一人分成两人） | 讯飞侧能力，无法规避；盲分对 2-3 人场景准确率较高 |

---

## 六、测试计划

1. **单测（新增）**
   - `result_parser.parse_sentences`：构造带 bg/ed/rl 的假 orderResult，验证提取与类型转换
   - rl 缺失 -> speakerId=None；bg/ed 字符串转 int
2. **单测（链路）**
   - enable_speaker=true 同步：responseContent 含 sentences
   - enable_speaker=true 异步：fetch completed 顶层含 sentences
   - 不传 enable_speaker：响应与现状字段级一致（回归断言）
3. **联调（真实讯飞）**
   - 直调路径：双人对话音频 + roleType=1，验证 rl 权限与输出
   - 网关路径：确认 options 透传
4. **回归**：现有 14 个用例 + 不传参数的真实调用（chat_completions/create/fetch）

---

## 七、改动文件清单与工作量

| 文件 | 改动 | 预估 |
|------|------|------|
| `result_parser.py` | 新增 parse_sentences | 30 分钟 |
| `iflytek_asr.py` | upload_audio/transcribe 加 role 参数，内部调用 parse_sentences | 30 分钟 |
| `gateway_asr.py` | options 加 role 参数，transcribe 返回结构 | 30 分钟 |
| `service.py` | 提取 enable_speaker，开启时返回 dict | 30 分钟 |
| `operation_router.py` | 提取参数、响应组装（同步 SSE + 异步 fetch） | 40 分钟 |
| `async_tasks.py` | dict 兼容 + task 存 sentences | 15 分钟 |
| `tests/` | 新增 3-4 个用例 | 30 分钟 |
| 联调验证 + 部署 | 直调/网关双路径验证 | 1 小时 |

**合计约 4 小时**（含联调）。

---

## 八、实施顺序

```
1. result_parser.parse_sentences + 单测（纯本地，可先行）
2. iflytek_asr / gateway_asr 参数透传
3. service / operation_router / async_tasks 链路打通
4. 本地全量单测回归
5. 【前置验证点】直调讯飞验证角色分离权限（风险①）
6. 部署 → 联调 enable_speaker=true 真实音频
7. 网关路径联调（风险②，若不通记录为二期）
```
