# 录音文件转写 Agent - 样例报文

> 供联调时直接复制使用，覆盖全部 4 个 operation 及错误场景。
> 认证方式：IAM（V11-HMAC-SHA256 签名），需使用华为云 SDK 或 `agentarts invoke` CLI。
> **200 成功响应统一为 SSE 事件流**（与 AgentArts 低码工作流运行时一致）：依次发送
> `workflow_started` -> `message` -> `workflow_finished` -> `end` 四个事件，
> 主 Agent 从 `workflow_finished.data.outputs.responseContent` 解析最终结果。
> 非 200（400/404/500）仍返回裸 JSON 错误体（`{"error":{...}}`）。

---

## 1. 请求头（所有请求通用）

```http
Content-Type: application/json
X-Hw-Agentarts-Session-Id: my-session-001
Authorization: <IAM V11-HMAC-SHA256 签名>
```

> ⚠️ **Session 亲和约束**：create_response 与 fetch_response 必须携带**同一个** `X-Hw-Agentarts-Session-Id`，否则任务查不到返回 404。

---

## 2. query_capabilities -- 能力查询

### 请求

```json
{
  "inputs": {
    "operation": "query_capabilities"
  }
}
```

### 响应 200（SSE 事件流，responseContent 为能力字典字符串）

```text
data: {"event":"workflow_started","data":{"start_time":1756800000000},"createdTime":1756800000000}
data: {"event":"message","data":{"text":"{'capabilities': {'chat_completions': True, 'responses_api': True, 'responses_get_fetch': True}}","index":1,"node_type":"End","node_name":"结束"},"createdTime":1756800000000}
data: {"event":"workflow_finished","data":{"status":{"code":0,"desc":"succeeded"},"outputs":{"responseContent":"{'capabilities': {'chat_completions': True, 'responses_api': True, 'responses_get_fetch': True}}"},"start_time":1756800000000,"end_time":1756800000001,"execution_id":"86f6d96538cc4b0bad653c6cc7ba6e99"},"createdTime":1756800000001}
data: {"event":"end","createdTime":1756800000001}
```

---

## 3. chat_completions -- 同步转写（阻塞至完成）

### 请求

```json
{
  "inputs": {
    "operation": "chat_completions",
    "file_url": ["https://asr-test-wav.obs.cn-southwest-2.myhuaweicloud.com/lfasr_%E6%B6%89%E6%94%BF.wav"],
    "model_config": [
      {
        "type": "offline_asr_upload",
        "provider": "openai-compatible",
        "endpoint": "https://convai-router.cn-southwest-2.myhuaweicloud.com/v2",
        "model_name": "ifasr",
        "auth_token": "Bearer eyJhbGciOiJIUzI1NiJ9..."
      }
    ]
  }
}
```

> `model_config` 置于请求顶层（与 `input` 平级）。写在 `input` 内亦可（顶层优先）。

### 响应 200（转写完成，阻塞约 30-60 秒；responseContent 为转写文本）

```text
data: {"event":"workflow_started","data":{"start_time":1756800000000},"createdTime":1756800000000}
data: {"event":"message","data":{"text":"中新经纬7月15日电国家统计局15日公布数据显示，今年以来国际环境更趋复杂严峻……","index":1,"node_type":"End","node_name":"结束"},"createdTime":1756800000000}
data: {"event":"workflow_finished","data":{"status":{"code":0,"desc":"succeeded"},"outputs":{"responseContent":"中新经纬7月15日电国家统计局15日公布数据显示，今年以来国际环境更趋复杂严峻……"},"start_time":1756800000000,"end_time":1756800000001,"execution_id":"16c75302c3794f27a727ad6c2a5ce2b5"},"createdTime":1756800000001}
data: {"event":"end","createdTime":1756800000001}
```

### 响应 500（转写失败）

```json
{
  "error": {
    "code": "E5001",
    "message": "转写失败: 下载音频失败",
    "type": "transcribe_failed"
  }
}
```

---

## 4. create_response -- 异步创建转写任务

### 请求

```json
{
  "inputs": {
    "operation": "create_response",
    "file_url": ["https://asr-test-wav.obs.cn-southwest-2.myhuaweicloud.com/lfasr_%E6%B6%89%E6%94%BF.wav"],
    "model_config": [
      {
        "type": "offline_asr_upload",
        "provider": "openai-compatible",
        "endpoint": "https://convai-router.cn-southwest-2.myhuaweicloud.com/v2",
        "model_name": "ifasr",
        "auth_token": "Bearer eyJhbGciOiJIUzI1NiJ9..."
      }
    ]
  }
}
```

### 响应 200（立即返回，不等转写；responseContent 携带 response_id，主 Agent 据此轮询 fetch_response）

```text
data: {"event":"workflow_started","data":{"start_time":1756800000000},"createdTime":1756800000000}
data: {"event":"message","data":{"text":"{'response_id': 'resp_91d80d9a-dcc1-4f7a-8766-e4207f7d99b2', 'status': 'in_progress'}","index":1,"node_type":"End","node_name":"结束"},"createdTime":1756800000000}
data: {"event":"workflow_finished","data":{"status":{"code":0,"desc":"succeeded"},"outputs":{"responseContent":"{'response_id': 'resp_91d80d9a-dcc1-4f7a-8766-e4207f7d99b2', 'status': 'in_progress'}"},"start_time":1756800000000,"end_time":1756800000001,"execution_id":"c509dc64380f46b08d53aebde46f1991"},"createdTime":1756800000001}
data: {"event":"end","createdTime":1756800000001}
```

---

## 5. fetch_response -- 查询异步转写结果

### 请求

```json
{
  "inputs": {
    "operation": "fetch_response",
    "response_id": "resp_91d80d9a-dcc1-4f7a-8766-e4207f7d99b2"
  }
}
```

> ⚠️ `X-Hw-Agentarts-Session-Id` 必须与 create_response 请求相同！

### 响应 200（已完成；responseContent 为转写文本，与 chat_completions 一致）

```text
data: {"event":"workflow_started","data":{"start_time":1756800000000},"createdTime":1756800000000}
data: {"event":"message","data":{"text":"中新经纬7月15日电国家统计局15日公布数据显示……","index":1,"node_type":"End","node_name":"结束"},"createdTime":1756800000000}
data: {"event":"workflow_finished","data":{"status":{"code":0,"desc":"succeeded"},"outputs":{"responseContent":"中新经纬7月15日电国家统计局15日公布数据显示……"},"start_time":1756800000000,"end_time":1756800000001,"execution_id":"caf0744e9ca546e3a4e76e0aba530308"},"createdTime":1756800000001}
data: {"event":"end","createdTime":1756800000001}
```

### 响应 200（进行中，需继续轮询；responseContent 为状态字符串）

```text
data: {"event":"workflow_started","data":{"start_time":1756800000000},"createdTime":1756800000000}
data: {"event":"message","data":{"text":"{'id': 'resp_91d80d9a-dcc1-4f7a-8766-e4207f7d99b2', 'object': 'response', 'status': 'in_progress'}","index":1,"node_type":"End","node_name":"结束"},"createdTime":1756800000000}
data: {"event":"workflow_finished","data":{"status":{"code":0,"desc":"succeeded"},"outputs":{"responseContent":"{'id': 'resp_91d80d9a-dcc1-4f7a-8766-e4207f7d99b2', 'object': 'response', 'status': 'in_progress'}"},"start_time":1756800000000,"end_time":1756800000001,"execution_id":"d5f08f1e2c3a4b5d6e7f8091a2b3c4d5e"},"createdTime":1756800000001}
data: {"event":"end","createdTime":1756800000001}
```

### 响应 200（转写失败；responseContent 为错误字符串）

```text
data: {"event":"workflow_started","data":{"start_time":1756800000000},"createdTime":1756800000000}
data: {"event":"message","data":{"text":"{'id': 'resp_91d80d9a-dcc1-4f7a-8766-e4207f7d99b2', 'object': 'response', 'status': 'failed', 'error': {'code': 'E5001', 'message': '转写失败: 讯飞API返回错误', 'type': 'transcribe_failed'}}","index":1,"node_type":"End","node_name":"结束"},"createdTime":1756800000000}
data: {"event":"workflow_finished","data":{"status":{"code":0,"desc":"succeeded"},"outputs":{"responseContent":"{'id': 'resp_91d80d9a-dcc1-4f7a-8766-e4207f7d99b2', 'object': 'response', 'status': 'failed', 'error': {'code': 'E5001', 'message': '转写失败: 讯飞API返回错误', 'type': 'transcribe_failed'}}"},"start_time":1756800000000,"end_time":1756800000001,"execution_id":"a1b2c3d4e5f60718293a4b5c6d7e8f901"},"createdTime":1756800000001}
data: {"event":"end","createdTime":1756800000001}
```

### 响应 404（不存在 / 过期 / 非本人）

```json
{
  "error": {
    "code": "E4006",
    "message": "响应不存在或已过期",
    "type": "response_not_found"
  }
}
```

---

## 6. 错误场景汇总

### 6.1 未知 operation -> 400 E4001

**请求：**
```json
{
  "inputs": {
    "operation": "do_something"
  }
}
```

**响应 400：**
```json
{
  "error": {
    "code": "E4001",
    "message": "未知的 operation: do_something",
    "type": "invalid_request"
  }
}
```

### 6.2 create_response 缺 input -> 400 E4001

**请求：**
```json
{
  "inputs": {
    "operation": "create_response"
  }
}
```

**响应 400：**
```json
{
  "error": {
    "code": "E4001",
    "message": "create_response 缺少必填参数 input",
    "type": "invalid_request"
  }
}
```

### 6.3 fetch_response 缺 response_id -> 400 E4001

**请求：**
```json
{
  "inputs": {
    "operation": "fetch_response"
  }
}
```

**响应 400：**
```json
{
  "error": {
    "code": "E4001",
    "message": "fetch_response 缺少必填参数 response_id",
    "type": "invalid_request"
  }
}
```

### 6.4 不存在的 response_id -> 404 E4006

**请求：**
```json
{
  "inputs": {
    "operation": "fetch_response",
    "response_id": "resp_not_exist"
  }
}
```

**响应 404：**
```json
{
  "error": {
    "code": "E4006",
    "message": "响应不存在或已过期",
    "type": "response_not_found"
  }
}
```

### 6.5 请求体非 JSON 对象 -> 400 E4001

**请求：**
```text
"just a string"
```

**响应 400：**
```json
{
  "error": {
    "code": "E4001",
    "message": "请求体必须是 JSON 对象",
    "type": "invalid_request"
  }
}
```

---

## 7. model_config 字段说明

| 字段 | 必填 | Agent 是否使用 | 说明 |
|---|---|---|---|
| 位置 | `inputs.model_config` | 是 | 在 inputs **里面** |
| `type` | 是 | ✅ 筛选 | 必须为 `offline_asr`，其他类型被忽略 |
| `provider` | 否 | ⬜ | 预留 |
| `endpoint` | 是 | ✅ 核心 | ConvAIRouter 地址，兼容带/不带 `/v2` 后缀 |
| `model_name` | 否 | ⬜ | 预留 |
| `auth_token` | 是 | ✅ 核心 | Bearer 认证，兼容带/不带 `Bearer ` 前缀 |

---

## 8. curl 快速测试命令

```bash
# 1. 能力查询
curl -s -X POST https://defaultgw-grstqnldg5.cn-southwest-2.huaweicloud-agentarts.com/runtimes/asr-agent/invocations \
  -H "Content-Type: application/json" \
  -H "X-Hw-Agentarts-Session-Id: my-sess-001" \
  -H "Authorization: <V11签名>" \
  -d '{"inputs":{"operation":"query_capabilities"}}'

# 2. 同步转写（阻塞约1分钟）
curl -s -X POST https://defaultgw-grstqnldg5.cn-southwest-2.huaweicloud-agentarts.com/runtimes/asr-agent/invocations \
  -H "Content-Type: application/json" \
  -H "X-Hw-Agentarts-Session-Id: my-sess-001" \
  -H "Authorization: <V11签名>" \
  -d '{"inputs":{"operation":"chat_completions","file_url":["<音频URL>"],"model_config":[{"type":"offline_asr","endpoint":"<router地址>","auth_token":"Bearer xxx"}]}}'

# 3. 异步创建
curl -s -X POST https://defaultgw-grstqnldg5.cn-southwest-2.huaweicloud-agentarts.com/runtimes/asr-agent/invocations \
  -H "Content-Type: application/json" \
  -H "X-Hw-Agentarts-Session-Id: my-sess-002" \
  -H "Authorization: <V11签名>" \
  -d '{"inputs":{"operation":"create_response","file_url":["<音频URL>"],"model_config":[{"type":"offline_asr","endpoint":"<router地址>","auth_token":"Bearer xxx"}]}}'

# 4. 查询结果（Session-Id 必须与创建时一致！）
curl -s -X POST https://defaultgw-grstqnldg5.cn-southwest-2.huaweicloud-agentarts.com/runtimes/asr-agent/invocations \
  -H "Content-Type: application/json" \
  -H "X-Hw-Agentarts-Session-Id: my-sess-002" \
  -H "Authorization: <V11签名>" \
  -d '{"inputs":{"operation":"fetch_response","response_id":"resp_xxx"}}'

# 5. 错误：不存在（预期 404）
curl -s -X POST https://defaultgw-grstqnldg5.cn-southwest-2.huaweicloud-agentarts.com/runtimes/asr-agent/invocations \
  -H "Content-Type: application/json" \
  -H "X-Hw-Agentarts-Session-Id: my-sess-001" \
  -H "Authorization: <V11签名>" \
  -d '{"inputs":{"operation":"fetch_response","response_id":"resp_not_exist"}}'
```
