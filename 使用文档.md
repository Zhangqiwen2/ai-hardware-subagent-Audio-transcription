# 录音文件转写智能体 - 使用文档

> 本文档详细说明如何在本地和云端使用录音文件转写智能体，包含每一步命令及其作用说明。

---

## 一、本地使用（在 Mac 上转写音频）

适用于：快速转写单个 WAV 文件，无需上云。

### 1.1 进入项目目录

```bash
cd /Users/zhangqiwen/work/agent/asr_agent
```

> **作用**：进入项目根目录。后续所有本地命令都在此目录下执行。项目包含 `main.py`（本地转写入口）、`iflytek_asr.py`（讯飞客户端）等。

### 1.2 安装依赖

```bash
pip3 install requests python-dotenv
```

> **作用**：安装本地转写所需的最小依赖。
> - `requests`：调用讯飞 API 的 HTTP 客户端
> - `python-dotenv`：从 `.env` 文件读取密钥
>
> 说明：本地直接转写（`main.py`）只需要这两个包。`agentarts-sdk` 仅云端部署时需要，本地可不安。

### 1.3 配置讯飞密钥

```bash
cp .env.example .env
```

> **作用**：复制密钥模板。然后编辑 `.env`，填入真实值：
> ```
> IFLYTEK_APP_ID=5cefcf5d
> IFLYTEK_API_KEY=2acb757703aa18fba126a5a0b83ff8ef
> IFLYTEK_API_SECRET=YTliMzFkNzVjMzcyMjY1MTFlYzc4Njcz
> ```
> 这三项从讯飞开放平台控制台获取。`config.py` 会在启动时自动读取 `.env`。

### 1.4 转写音频文件

```bash
python3 main.py /path/to/你的音频.wav
```

> **作用**：执行本地转写。流程为：
> 1. `config.py` 读取 `.env` 中的讯飞密钥
> 2. `service.py` 解析音频文件路径
> 3. `iflytek_asr.py` 用 WAV 时长 -> HMAC-SHA1 签名 -> 上传音频到讯飞 -> 轮询查询结果
> 4. `result_parser.py` 把多层嵌套 JSON 解析成纯文本
> 5. 打印转写结果
>
> **限制**：仅支持 WAV（16kHz/8kHz、16bit、单声道，≤5h、≤500MB）。
>
> 示例（用项目自带音频）：
> ```bash
> python3 main.py ../Ifasr_llm/audio/lfasr_涉政.wav
> ```

### 1.5 测试解析逻辑（不耗额度、不联网）

```bash
python3 tests/test_local.py
```

> **作用**：用讯飞文档的示例响应验证 `result_parser.py` 能否正确提取文本。输出 `喂你好` 并 `[PASS]` 即通过。不调用讯飞 API，不消耗额度。

---

## 二、云端部署（首次，在 ECS 上操作）

适用于：把转写能力部署成云端服务，供外部调用。

### 2.1 准备 ECS

在华为云控制台购买**鲲鹏 ARM64** ECS：
- 区域：西南-贵阳一（cn-southwest-2）
- 架构：鲲鹏 ARM64（**必须**，X86 镜像调用会失败）
- 镜像：Ubuntu 22.04
- 公网 IP：必须有

> **作用**：提供构建 Docker 镜像 + 部署的环境。记下 ECS 公网 IP。

### 2.2 上传代码并初始化 ECS 环境（在 Mac 执行）

```bash
cd /Users/zhangqiwen/work/agent/asr_agent
bash scripts/deploy.sh root@<ECS公网IP>
```

> **作用**：一条命令完成：
> 1. `rsync` 把项目代码上传到 ECS 的 `~/asr_agent/`
> 2. 检测到 ECS 首次部署，远程执行 `scripts/init-ecs.sh`：安装 Python3、Docker、配置镜像加速、创建虚拟环境、安装 `agentarts-sdk`
> 3. 完成后提示重新登录 ECS（让 docker 组生效）
>
> 脚本结束后会打印后续手动步骤。

### 2.3 重新登录 ECS 并配置凭证

```bash
ssh root@<ECS公网IP>
```

登录后：

```bash
cd ~/asr_agent
source ~/venv/bin/activate
```

> **作用**：进入项目目录，激活 Python 虚拟环境（`agentarts` CLI 装在这里）。

```bash
export HUAWEICLOUD_SDK_AK='你的华为云AK'
export HUAWEICLOUD_SDK_SK='你的华为云SK'
```

> **作用**：设置华为云访问密钥，`agentarts` CLI 用它鉴权（构建镜像、推 SWR、创建运行时都需要）。
> AK/SK 从华为云「我的凭证 -> 访问密钥」下载的 CSV 获取。
>
> **注意**：必须用 ASCII 直引号 `'`，不能是弯引号 `'`（会导致签名 401）。

为避免每次登录都设，持久化到 `~/.bashrc`：

```bash
echo 'export HUAWEICLOUD_SDK_AK="你的AK"' >> ~/.bashrc
echo 'export HUAWEICLOUD_SDK_SK="你的SK"' >> ~/.bashrc
source ~/.bashrc
```

### 2.4 配置智能体（首次）

```bash
agentarts configure --entrypoint app:app
```

> **作用**：配置部署参数。`--entrypoint app:app` 指定入口为 `app.py` 文件里的 `app` 对象（`AgentArtsRuntimeApp` 实例）。
> 交互式填写：智能体名称（`asr-agent`）、区域（`cn-southwest-2`）、依赖文件（`requirements.txt`）、SWR 镜像组织名（用默认）。
> 配置存入 `.agentarts_config.yaml`。

### 2.5 构建并部署

```bash
export DOCKER_BUILDKIT=0
agentarts launch
```

> **作用**：
> - `DOCKER_BUILDKIT=0`：关闭 BuildKit，避免 Docker 27+ 与华为云 SWR 的 OCI 格式不兼容问题。
> - `agentarts launch` 自动完成 5 步：
>   1. 构建 ARM64 Docker 镜像（`python:3.10-slim` 基础镜像 + 项目代码）
>   2. 在 SWR 创建镜像组织/仓库
>   3. 获取 SWR 推送凭证并登录
>   4. 推送镜像到 SWR
>   5. 创建 AgentArts 运行时（返回访问域名）
>
> 成功后会输出 **Access Endpoint**（如 `defaultgw-grstqnldg5.cn-southwest-2.huaweicloud-agentarts.com`），这就是云端调用地址。

### 2.6 控制台配置运行时（关键）

在华为云 AgentArts 控制台 ->「部署运行 -> 智能体运行时 -> asr-agent」详情页：

**配置环境变量**（运行时用这些调讯飞 API）：
```
IFLYTEK_APP_ID=5cefcf5d
IFLYTEK_API_KEY=2acb757703aa18fba126a5a0b83ff8ef
IFLYTEK_API_SECRET=YTliMzFkNzVjMzcyMjY1MTFlYzc4Njcz
```

**出网网络**：选 `PUBLIC`（运行时需走公网访问讯飞 API）。

> **作用**：环境变量让运行时内的代码能读到讯飞密钥；公网出口让运行时能访问 `office-api-ist-dx.iflyaisol.com`。两者缺一不可。

---

## 三、云端调用（日常使用）

以下命令在 ECS 上执行（`source ~/venv/bin/activate` 后）。

### 3.1 冒烟测试（验证服务是否正常）

```bash
agentarts invoke '{}' -a asr-agent
```

> **作用**：向云端运行时发送空 payload。
> - `agentarts invoke`：调用命令，自动用 AK/SK 做 IAM 签名（无需手写签名）
> - `'{}'`：空 JSON payload
> - `-a asr-agent`：指定运行时名称
>
> **预期返回**（服务正常）：
> ```json
> {"response":"转写失败: 无法从 payload 解析音频来源...","status":"error"}
> ```
> 收到这个 200 响应 = 端点 + 签名 + 服务全通（空 payload 业务层正确报错）。

### 3.2 转写公网音频（推荐方式）

**第一步：把 WAV 上传到 OBS 拿公网 URL**

在华为云 OBS 控制台创建桶（如 `asr-test-wav`，区域 cn-southwest-2，设公开读），上传 WAV 文件，复制对象 URL，形如：
```
https://asr-test-wav.obs.cn-southwest-2.myhuaweicloud.com/你的音频.wav
```

> **作用**：运行时需通过公网 URL 下载音频。OBS 与运行时同区域，下载最快。也可用任何公网可下载的 WAV URL。

**第二步：调用转写**

```bash
agentarts invoke '{"audio_url":"https://asr-test-wav.obs.cn-southwest-2.myhuaweicloud.com/你的音频.wav"}' -a asr-agent
```

> **作用**：运行时收到 `audio_url` 后：
> 1. 下载音频到运行时临时目录
> 2. 调讯飞 API 上传 + 轮询转写
> 3. 解析结果返回纯文本
>
> **预期返回**：
> ```json
> {"response":"转写出来的文本...","status":"success"}
> ```

### 3.3 转写运行时内文件（需开启文件传输）

> 注意：当前运行时未开启文件传输（且创建后不能修改）。如需此方式，要新建一个 `file_transfer_config.enabled=true` 的运行时。开启后用法：

```bash
# 上传 WAV 到运行时容器（默认存 /tmp/）
agentarts runtime upload-files -a asr-agent -s sess-001 -f ~/你的音频.wav
```

> **作用**：把本地文件传到运行时容器的 `/tmp/` 目录。
> - `-s sess-001`：会话 ID
> - `-f`：本地文件路径

```bash
# 用文件路径转写
agentarts invoke '{"file_path":"/tmp/你的音频.wav"}' -a asr-agent
```

> **作用**：运行时直接读取容器内 `/tmp/你的音频.wav` 进行转写，无需公网下载。

### 3.4 调用命令的完整选项

```bash
agentarts invoke --help
```

常用选项：
| 选项 | 作用 |
|---|---|
| `-a, --agent` | 运行时名称（默认用 default_agent） |
| `-m, --mode` | `cloud`（默认，调云端）/ `local`（调本地 Docker） |
| `-s, --session` | 会话 ID（有状态对话用） |
| `-e, --endpoint` | 指定访问方式名称 |
| `--timeout` | 超时秒数（默认 900） |

---

## 四、更新代码并重新部署

代码修改后，在 Mac 上一条命令同步并部署：

```bash
cd /Users/zhangqiwen/work/agent/asr_agent
bash scripts/deploy.sh root@<ECS公网IP> --launch
```

> **作用**：
> 1. `rsync` 把修改后的代码同步到 ECS（`--delete` 保持一致，自动排除缓存/venv）
> 2. 远程 `pip install -r requirements.txt` 更新依赖
> 3. 远程 `agentarts launch` 重新构建镜像 + 推送 + 更新运行时
>
> `configure` 只需首次做一次，后续更新直接用这条命令。

---

## 五、请求/响应格式说明

### 5.1 请求 payload（JSON）

支持以下音频来源字段（任选其一）：

| 字段 | 说明 | 示例 |
|---|---|---|
| `audio_url` | 公网可下载的 WAV URL | `{"audio_url":"https://.../x.wav"}` |
| `file_path` | 运行时容器内文件路径（需文件传输） | `{"file_path":"/tmp/x.wav"}` |
| `message` / `prompt` / `input` | 字符串，值为路径或 URL | `{"message":"/tmp/x.wav"}` |

可选参数：
| 字段 | 说明 | 示例 |
|---|---|---|
| `language` | 语言（默认 `autodialect`） | `{"audio_url":"...","language":"autodialect"}` |
| `pd` | 领域（`finance`/`medical`/`tech` 等，空则不传） | `{"audio_url":"...","pd":"finance"}` |

### 5.2 响应格式

```json
{"response": "转写文本...", "status": "success"}   // 成功
{"response": "转写失败: 原因...", "status": "error"}  // 失败
```

### 5.3 HTTP 接口（直接 curl 调用，需 IAM 签名）

运行时也支持标准 HTTP 调用，但因认证方式是 IAM，需 AK/SK 签名：

```
POST https://defaultgw-grstqnldg5.cn-southwest-2.huaweicloud-agentarts.com/runtimes/asr-agent/invocations
请求头：X-Hw-Agentarts-Session-Id、Authorization（IAM签名）、Content-Type: application/json
```

> IAM 签名较复杂（V11-HMAC-SHA256），**推荐用 `agentarts invoke` CLI 自动处理**，无需手写签名。

---

## 六、常见问题速查

| 问题 | 原因 | 解决 |
|---|---|---|
| `agentarts invoke` 报 "Could not find valid credentials" | 当前 shell 未设 AK/SK | `export HUAWEICLOUD_SDK_AK/SK` 或确认已写入 `~/.bashrc` 并 `source` |
| 返回 `讯飞密钥未配置` | 运行时环境变量没配 | 控制台给运行时加 `IFLYTEK_*` 环境变量 |
| 返回 `下载音频失败` | audio_url 不可访问 | 确认 URL 公网可下载（OBS 设公开读） |
| 转写超时 | 音频太长或网络慢 | 调大 `--timeout`，或检查讯飞服务状态 |
| `agentarts launch` AK/SK 报错 | AK/SK 无权限或含弯引号 | 用主账号 AK/SK，确保 ASCII 直引号 |
| 镜像构建 pip 慢 | 容器内用默认 PyPI | requirements.txt 顶部已有华为云 PyPI 镜像 |
| `File transfer config cannot be modified` | 文件传输创建后不能改 | 新建运行时开启，或改用 audio_url |

---

## 七、关键信息备忘

| 项 | 值 |
|---|---|
| 运行时名称 | asr-agent |
| Runtime ID | 8d043a4a-c5ff-4576-8f5d-2e3fbd69257a |
| 访问域名 | defaultgw-grstqnldg5.cn-southwest-2.huaweicloud-agentarts.com |
| 区域 | cn-southwest-2（西南-贵阳一） |
| 认证方式 | IAM（用 `agentarts invoke` 调用） |
| ECS | ecs-a319-53b2 |
| OBS 测试桶 | asr-test-wav（公开读） |

更多架构与实现细节见 `实现文档.md`，部署排错见 `DEPLOY.md`。
