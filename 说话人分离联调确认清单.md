# 说话人分离 + 时间戳 — 联调确认清单

> 背景：录音转写 Agent 计划支持说话人区分（speakerId）和逐段时间戳。
> 依据讯飞录音文件转写大模型 API 的 `roleType`/`roleNum` 参数（通用角色分离 + 盲分），
> 每个分段可返回「文本 + 角色编号 + 开始/结束毫秒时间戳」。
>
> 本清单列出**需要主 Agent 侧确认的接口契约问题**，请逐项勾选/填写，
> 确认后我们按结论开发，避免字段名和格式返工。
>
> 提出方：录音转写 Agent（asr-agent）
> 日期：2026-09-01

---

## 拟定的接口变更（供参考，以下问题均基于此）

**请求**（新增可选字段，不传 = 现状完全不变）：

```json
{
  "inputs": {
    "operation": "chat_completions",
    "file_url": ["https://obs.../audio.wav"],
    "enable_speaker": true
  }
}
```

**同步响应**（responseContent 新增 sentences 字段）：

```json
{
  "message": "我们下周确认方案。好的，我来记录下来。",
  "sentences": [
    {"text": "我们下周确认方案。", "speakerId": 1, "beginTimeMs": 4220, "endTimeMs": 6380},
    {"text": "好的，我来记录下来。", "speakerId": 2, "beginTimeMs": 6710, "endTimeMs": 9210}
  ]
}
```

**异步响应**（fetch_response completed 顶层新增 sentences）：

```json
{
  "id": "resp_xxx",
  "status": "completed",
  "output": [...],
  "sentences": [
    {"text": "我们下周确认方案。", "speakerId": 1, "beginTimeMs": 4220, "endTimeMs": 6380}
  ]
}
```

---

## 一、接口契约问题（必须确认）

### Q1. `enable_speaker` 开关由谁控制？

| 选项 | 说明 | 影响 |
|------|------|------|
| A | 主 Agent 按需传 `enable_speaker: true`，默认 false | 主 Agent 需改请求代码；不开分离的调用零影响 |
| B | 我们默认全开（所有转写都带分离） | 主 Agent 请求不用改；但所有转写都消耗分离能力 |

**我方建议**：A（可控、可灰度）。

> 主 Agent 答复：＿＿＿＿

---

### Q2. 字段命名风格：camelCase 还是 snake_case？

sentences 内部字段两种风格：

| 风格 | 示例 |
|------|------|
| camelCase | `{"text", "speakerId", "beginTimeMs", "endTimeMs"}` |
| snake_case | `{"text", "speaker_id", "begin_ms", "end_ms"}` |

**我方建议**：camelCase（与贵方样例协议一致）。

> 主 Agent 答复：＿＿＿＿

---

### Q3. 响应结构：只要 sentences 数组，还是要对齐完整 vendor 协议？

贵方提供的样例报文包含 `taskId` / `requestId` / `status` / `usage.durationSeconds` / `fullText` / `sentences` 等字段：

| 选项 | 说明 |
|------|------|
| A | 我们保持现有响应结构（OpenAI 格式 + message 文本），仅**新增 sentences 字段**，由主 Agent 自行组装 vendor 协议 |
| B | 我们完整返回贵方 vendor 协议格式（taskId/fullText/usage 等全套），响应结构大改 |

**我方建议**：A（改动小、风险低，vendor 协议由主 Agent 统一组装）。

> 主 Agent 答复：＿＿＿＿

---

### Q4. 同步路径 sentences 的位置与解析

拟定放在 SSE 的 `responseContent` 里：`{"message": 全文, "sentences": [...]}`。

**需确认**：
- 主 Agent 对 responseContent 的解析是否兼容新增字段（宽松解析 vs 严格校验）？
- sentences 是否也从 `data.outputs.responseContent.sentences` 读取？

> 主 Agent 答复：＿＿＿＿

---

### Q5. 异步路径 sentences 的位置

拟定在 `fetch_response` completed 响应**顶层**加 `sentences`（与 `error_code` 同级）。

**需确认**：主 Agent 从该位置读取是否可行？

> 主 Agent 答复：＿＿＿＿

---

## 二、语义与降级问题（最好确认）

### Q6. speakerId 语义确认

讯飞角色分离只返回**编号**（1/2/3…），无法关联真实人名（如"张三"）。

**需确认**：主 Agent 接受纯编号（展示为"说话人 1/说话人 2"）吗？
若需真实人名，要走声纹方案（提前注册声纹），复杂度和工期都会增加，建议二期评估。

> 主 Agent 答复：＿＿＿＿

---

### Q7. 分离失败时的降级策略

若 `enable_speaker=true` 但讯飞侧分离失败/无权限：

| 选项 | 行为 |
|------|------|
| A | 返回错误（E5001），主 Agent 明确感知失败 |
| B | 静默降级：正常返回纯文本（message 有值），`sentences` 为 null |

**我方建议**：B（转写不中断，体验更好）。

> 主 Agent 答复：＿＿＿＿

---

### Q8. 分段粒度

| 粒度 | 说明 |
|------|------|
| VAD 分段（一期） | 讯飞原生分段，一段可能几十秒、含多句话 |
| 句号切分（二期） | 在 VAD 分段基础上按标点再切，粒度到完整句子 |

**需确认**：主 Agent 的展示场景（字幕/对话气泡/会议纪要？）需要哪种粒度？

> 主 Agent 答复：＿＿＿＿

---

### Q9. 逐句 language 字段

贵方样例中 sentences 有 `language` 字段，但**讯飞只提供文件级语种，无逐句语种**。

**需确认**：该字段去掉、或统一填文件级语种？

> 主 Agent 答复：＿＿＿＿

---

## 三、联调安排

| 事项 | 说明 | 负责方 |
|------|------|--------|
| 讯飞角色分离权限验证 | roleType=1 直调验证账号权限 | 转写 Agent |
| ConvAIAgent 网关透传确认 | 网关路径的 options 需透传 roleType/roleNum | 网关维护方 |
| 解析代码对齐 | 确认字段名后主 Agent 出解析样例，双方按此联调 | 双方 |
| 联调时间 | 待定（建议字段确认后 1-2 个工作日内） | 双方 |

---

## 附：speakerId 取值说明

- 类型：int（讯飞返回字符串数字，我方转换）
- 开启分离且分离成功：1、2、3…（盲分自动确定人数，最多 10）
- 某分段无角色信息（如未开启分离/分离失败）：`null`
