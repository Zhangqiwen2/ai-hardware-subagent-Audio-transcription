# 录音文件转写 Agent 接口定义文档

| 项 | 值 |
|---|---|
| 文档版本 | v1.3 |
| 更新日期 | 2026-08-17 |
| 依据规格 | FE2026081400138 独立 Agent 运行时接口规则（spec 5.18 / 6.12） |
| Agent 类型 | AgentArts 独立垂域 Agent（录音转写） |
| 能力模式 | **同步 + 异步**（chat_completions / create_response / fetch_response 均支持） |

---

## 1. 概述

录音文件转写 Agent 接收音频文件 URL，调用讯飞离线语音转写能力（经 ConvAIRouter 网关），返回转写纯文本。对外仅暴露一个统一执行接口，通过请求体中的 `operation` 字段路由到四种操作。

### 1.1 接口清单

| # | operation | 模式 | 功能 |
|---|---|---|---|
| 1 | `query_capabilities` | 元操作 | 查询能力声明 |
| 2 | `chat_completions` | 同步（阻塞） | 同步转写，阻塞至完成后返回 chat.completion |
| 3 | `create_response` | 异步 | 创建转写任务，立即返回 response_id |
| 4 | `fetch_response` | 异步 | 按 response_id 查询转写结果 |

### 1.2 转写链路

```
调用方(ConvAIAgent) ──POST /invocations──> 转写Agent ──Bearer──> ConvAIRouter(/v2/upload,/v2/getResult) ──> 讯飞
```

- Agent 从 `model_config` 中获取 ConvAIRouter 的 `endpoint` 与 `auth_token`（由调用方注入）
- Agent **不持有**讯飞密钥；讯飞鉴权（HMAC-SHA1）由 ConvAIRouter 侧完成

---

## 2. 通用约定

### 2.1 请求地址

```http
POST https://{gateway_domain}/runtimes/{runtime_name}/invocations
Content-Type: application/json
```

- `{gateway_domain}`：AgentArts 平台分配的访问域名
- `{runtime_name}`：运行时名称

### 2.2 请求头

| Header | 必填 | 说明 |
|---|---|---|
| `Content-Type` | 是 | `application/json` |
| `X-Hw-Agentarts-Session-Id` | 是 | 会话 ID。**关键约束：运行时按 Session 亲和路由（同 Session 同实例）**，create_response 与 fetch_response 必须携带**同一个** Session-Id，否则任务查不到返回 404 E4006（已实测验证） |
| `Authorization` | 是 | 平台入站认证（运行时为 IAM 认证；调用方经平台透传，无需自行签名） |
| `X-Hw-Agentgateway-User-Id` | 否 | 用户标识；Agent 用作异步任务归属校验（owner） |

### 2.3 请求体（统一信封）

```json
{
  "inputs": {
    "operation": "query_capabilities | chat_completions | create_response | fetch_response",
    "file_url": ["..."],
    "model_config": [ ... ],
    "response_id": "resp_xxx"
  }
}
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `operation` | string | 否 | 缺省或空串时**默认 `chat_completions`**（spec 5.18.1 规则3） |
| `inputs.file_url` | array | 条件 | `chat_completions`/`create_response` 必填；**数组**，取第一个元素 |
| `model_config` | array | 条件 | 转写模型配置，**标准位置在顶层（与 input 平级）**；`chat_completions`/`create_response` 必填（见第4节） |
| `response_id` | string | 条件 | `fetch_response` 必填 |

### 2.4 统一错误格式

```json
{
  "error": {
    "code": "E4001",
    "message": "错误详情",
    "type": "invalid_request"
  }
}
```

### 2.5 错误码表

| 错误码 | HTTP | type | 说明 | 触发场景 |
|---|---|---|---|---|
| E4001 | 400 | invalid_request | 参数非法 | operation 未知枚举；请求体非 JSON 对象；chat_completions/create_response 缺 `input`；fetch_response 缺 `response_id` |
| E4007 | 400 | capability_not_supported | 能力不支持 | operation 对应能力标志位为 false（本 Agent 三项能力均声明支持，正常不触发） |
| E4006 | 404 | response_not_found | 响应不存在或已过期 | response_id 不存在 / 过期(>24h) / 非该用户。三种情况**统一返回**，不泄露存在性 |
| E5001 | 500 | transcribe_failed | 转写失败 | 同步转写异常；（异步场景失败返回 200 + `status:"failed"`，见 4.3） |

---

## 3. 接口详细定义

### 3.1 query_capabilities —— 能力查询

**Request**
```json
{ "operation": "query_capabilities" }
```

**Response `200`**
```json
{
  "capabilities": {
    "chat_completions": true,
    "responses_api": true,
    "responses_get_fetch": true
  }
}
```

**约束**（spec 5.18.1 规则1/规则7）：
- 响应体**仅含** `capabilities` 对象，不含任何其他元数据字段
- 元操作：不做能力校验、无业务副作用，所有 Agent 必须支持

**capabilities 字段**

| 字段 | 类型 | 值 | 说明 |
|---|---|---|---|
| chat_completions | boolean | `true` | 支持同步对话（含无 operation 默认路由） |
| responses_api | boolean | `true` | 支持异步创建响应 |
| responses_get_fetch | boolean | `true` | 支持查询异步响应（依赖 responses_api=true，启动自检保证） |

---

### 3.2 chat_completions —— 同步转写

**Request**
```json
{
  "inputs": {
    "operation": "chat_completions",
    "file_url": ["https://obs.../audio.mp3"],
    "model_config": [
      {
        "type": "offline_asr_upload",
        "provider": "openai-compatible",
        "endpoint": "https://convai-router/v2",
        "model_name": "ifasr",
        "auth_token": "Bearer <token>"
      }
    ]
  }
}
```

> **默认路由**：请求不携带 `operation`（或为空）且携带 `input` 时，等同本接口（spec 规则3）。

**Response `200`** —— OpenAI chat.completion 对象
```json
{
  "id": "chatcmpl-3f2ac9e0-8d5b-4f2a-9c1e-7a6b5d4e3f2a",
  "object": "chat.completion",
  "choices": [
    {
      "index": 0,
      "message": { "role": "assistant", "content": "转写出来的纯文本..." },
      "finish_reason": "stop"
    }
  ]
}
```

**Response `500 E5001`**（转写失败）
```json
{ "error": { "code": "E5001", "message": "转写失败: <原因>", "type": "transcribe_failed" } }
```

**行为说明**：
- **同步阻塞**：下载音频 -> 调 ConvAIRouter 上传 -> 轮询至转写完成 -> 返回。总耗时 ≈ 音频下载 + 转写（约 0.3~1 倍音频时长）+ 轮询间隔
- **调用方超时建议**：按音频时长 1 倍以上设置；长音频建议改用异步接口（3.3/3.4）

---

### 3.3 create_response —— 创建异步转写任务

**Request**
```json
{
  "inputs": {
    "operation": "create_response",
    "file_url": ["https://obs.../audio.mp3"],
    "model_config": [
      {
        "type": "offline_asr_upload",
        "provider": "openai-compatible",
        "endpoint": "https://convai-router/v2",
        "model_name": "ifasr",
        "auth_token": "Bearer <token>"
      }
    ]
  }
}
```

**Response `200`**
```json
{
  "response_id": "resp-格式见下方说明",
  "status": "in_progress"
}
```

**行为说明**：
- `response_id` 格式：`resp_` + UUID4（如 `resp_3f2ac9e0-8d5b-4f2a-9c1e-7a6b5d4e3f2a`）
- 立即返回，转写由后台线程异步执行
- 若请求头携带 `X-Hw-Agentgateway-User-Id`，该任务绑定此用户；他人 fetch 返回 404 E4006
- **有效期 24h**（spec：过期 >24h 返回 E4006）
- **⚠️ Session 亲和约束（实测）**：任务存在创建请求所命中实例的内存中，运行时按 `X-Hw-Agentarts-Session-Id` 路由。**fetch_response 必须与 create_response 使用同一个 Session-Id**，否则打到别的实例返回 404 E4006

---

### 3.4 fetch_response —— 查询异步转写结果

**Request**
```json
{
  "inputs": {
    "operation": "fetch_response",
    "response_id": "resp_3f2ac9e0-8d5b-4f2a-9c1e-7a6b5d4e3f2a"
  }
}
```

**Response**

| 场景 | HTTP | 响应 |
|---|---|---|
| 已完成 | 200 | 完整 OpenAI response 对象（见下） |
| 进行中 | 200 | `{"id":"resp_xxx","object":"response","status":"in_progress"}` |
| 转写失败 | 200 | `{"id":"resp_xxx","object":"response","status":"failed","error":{"code":"E5001","message":"...","type":"transcribe_failed"}}` |
| 不存在/过期/非本人 | 404 | E4006 |

**已完成响应**（spec 5.18.1 规则4：完整 OpenAI response 对象）
```json
{
  "id": "resp_3f2ac9e0-8d5b-4f2a-9c1e-7a6b5d4e3f2a",
  "object": "response",
  "status": "completed",
  "output": [
    {
      "type": "message",
      "role": "assistant",
      "content": [
        { "type": "output_text", "text": "转写出来的纯文本..." }
      ]
    }
  ]
}
```

> 注：`status:"failed"` 为本 Agent 扩展（spec 仅定义 completed/in_progress/404），联调需与平台对齐透传策略。

---

## 4. input 数据模型（转写请求）

### 4.1 标准结构（联调对齐）

```json
{
  "operation": "create_response",
  "input": { "file_url": "https://obs.../audio.mp3" },
  "model_config": [ { "type": "offline_asr_upload", ... } ]
}
```

| 位置 | 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `input` | `file_url` | string | 是 | 音频文件公网 URL（Agent 下载后转写） |
| 顶层 | `model_config` | array | 是 | 模型配置列表，**标准位置在顶层（与 input 平级）**，须含 `type=offline_asr` 项 |

### 4.2 model_config[].offline_asr 结构

| 字段 | 类型 | 必填 | Agent 是否使用 | 说明 |
|---|---|---|---|---|
| `type` | string | 是 | ✅ 筛选依据 | 必须为 `offline_asr`，否则该项被忽略 |
| `provider` | string | 否 | ⬜ 仅解析 | 预留（如 `openai-compatible`） |
| `endpoint` | string | 是 | ✅ 核心 | ConvAIRouter 网关地址。兼容带/不带 `/v2` 后缀，Agent 拼接 `/v2/upload`、`/v2/getResult` |
| `model_name` | string | 否 | ⬜ 仅解析 | 预留（ConvAIRouter 路由用） |
| `auth_token` | string | 是 | ✅ 核心 | Bearer 认证 token。兼容带/不带 `Bearer ` 前缀，Agent 放入 `Authorization` 头 |

### 4.3 兼容写法

| 写法 | 说明 |
|---|---|
| `model_config` 写在 `input` 内 | 兼容旧格式；顶层有 model_config 时以**顶层为准**（顶层优先） |
| `input` 直接为 URL 字符串 | 等价 `{"file_url": "<url>"}`（顶层 model_config 照常生效） |
| `input.file_path` / `input.audio_url` | 旧格式兼容（运行时容器内路径/旧字段名） |
| 顶层 `file_path` / `message` / `prompt` | 旧格式兼容（本地测试用） |

---

## 5. 约束与限制

| 项 | 值 | 说明 |
|---|---|---|
| 音频格式 | wav / mp3 / opus / flac / ogg / pcm / speex | 非 wav 自动关闭时长校验（由讯飞侧计算时长） |
| 音频时长 | ≤ 5 小时 | 讯飞侧限制 |
| 文件大小 | ≤ 500 MB | 讯飞侧限制 |
| response_id 有效期 | 24 小时 | 过期 fetch 返回 404 E4006 |
| 任务存储 | **内存态（实例内）** | Runtime 重启后任务丢失，fetch 返回 404 E4006（安全失败，已接受） |
| **Session 亲和** | **create 与 fetch 必须同 Session-Id** | 运行时按 Session 路由到实例（2026-08-18 云端实测确认：同 session 全流程通，跨 session 必 404） |
| 任务数上限 | 1000 | 超限先清过期，仍满则丢弃最旧任务 |
| 异步轮询间隔 | 10 秒（可配 `IFLYTEK_POLL_INTERVAL`） | Agent 轮询 ConvAIRouter 的间隔 |
| 轮询最长等待 | 3600 秒（可配 `IFLYTEK_POLL_MAX_WAIT`） | 超时报错 |
| 同步阻塞时长 | ≈ 0.3~1 倍音频时长 | 长音频建议走异步 |
| 并发 | 多请求互不影响 | 同步 handler 线程池 + 异步后台线程 |

---

## 6. 运行时路由规则（spec 5.18.1 双层校验）

```
请求进入
  ↓
① operation 缺失/空 ──────────────> 默认 chat_completions
  ↓
② operation 合法性（枚举内？） ──否──> 400 E4001
  ↓ 是
③ query_capabilities？ ───────────> 200 {capabilities}（不做能力校验）
  ↓ 否
④ capability 匹配（标志位=true？）─否──> 400 E4007
  ↓ 是
⑤ chat_completions ──> 同步转写（阻塞）──> 200 chat.completion / 500 E5001
   create_response ──> 创建任务 ──────> 200 {response_id, in_progress}
   fetch_response ──> 查任务 ────────> 200 response对象 / 404 E4006
```

---

## 7. 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-08-17 | 按 FE2026081400138 初版：纯异步（chat_completions=false） |
| v1.1 | 2026-08-17 | spec 对齐：HTTP 状态码（E4006->404）、fetch 完成返回 OpenAI response 对象、query_capabilities 仅含 capabilities |
| v1.2 | 2026-08-17 | **需求变更：支持同步 chat_completions**（capabilities 全 true；新增同步转写路由与 chat.completion 响应；新增 E5001） |
| v1.3 | 2026-08-17 | **接口格式对齐：`model_config` 标准位置移至请求顶层**（与 input 平级，顶层优先；input 内写法保留兼容） |
