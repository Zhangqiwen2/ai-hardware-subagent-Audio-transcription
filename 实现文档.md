# 录音文件转写智能体 - 实现文档

> 基于讯飞「录音文件转写大模型」API，封装为华为云 AgentArts（智果）平台高代码智能体运行时。
> 接收 WAV 音频，返回转写纯文本。

文档版本：v1.1 ｜ 更新日期：2026-08-06

---

## 一、项目概述

### 1.1 项目目标
将讯飞录音文件转写能力，以 AgentArts 高代码运行时形态封装为云端可调用的智能体服务，实现「音频文件 -> 纯文本」的标准化转换。

### 1.2 技术栈
| 组件 | 选型 | 用途 |
|---|---|---|
| 开发语言 | Python 3.10+ | |
| 平台 SDK | agentarts-sdk | AgentArtsRuntimeApp 运行时框架（基于 Starlette） |
| 转写引擎 | 讯飞录音文件转写大模型 API | `office-api-ist-dx.iflyaisol.com` |
| HTTP 客户端 | requests | 调用讯飞 API |
| 配置管理 | python-dotenv + 环境变量 | 本地 `.env` + 平台注入 |
| 部署形态 | Docker 镜像（ARM64） | 推送华为云 SWR，AgentArts 托管 |

### 1.3 运行时形态
- **高代码运行时**：Docker 镜像 + 启动命令 `python -m app`，监听 8080
- **入栈协议**：HTTP（`/invocations` POST、`/ping` GET 由 SDK 提供）
- **出站网络**：`PUBLIC`（需访问讯飞公网 API）
- **区域**：cn-southwest-2（西南-贵阳一）

---

## 二、总体开发方案

### 2.1 开发模式
采用 AgentArts 高代码开发的「代码云上托管」模式：本地自主开发转写核心逻辑，打包为 Docker 镜像，平台仅提供运行时托管与可观测能力。不依赖平台记忆/网关等组件，代码完全自主可控。

### 2.2 技术选型原则
- **平台合规优先**：使用官方 `agentarts-sdk`（`AgentArtsRuntimeApp`）而非裸 Web 框架，确保 `/invocations`、`/ping` 入栈契约合规
- **业务与平台解耦**：转写逻辑独立于 SDK（`service.py`），本地与云端复用同一套代码
- **最小依赖**：WAV 时长用内置 `wave` 模块计算，无需 ffmpeg 等额外依赖
- **复用与修正并重**：基于讯飞官方示例移植，按 API 文档修正不一致处

### 2.3 开发阶段
| 阶段 | 内容 | 主要产出 |
|---|---|---|
| ① 本地核心开发 | 讯飞签名/上传/轮询 + 结果解析 | `iflytek_asr.py`、`result_parser.py` |
| ② SDK 标准封装 | 接入 AgentArtsRuntimeApp，业务解耦 | `app.py`、`service.py` |
| ③ 容器化部署 | 依赖清单 + `agentarts launch` 上云 | SWR 镜像、运行时 |
| ④ 联调验证 | 本地实测 + 云端调用 | 转写结果验证 |

### 2.4 关键决策
| 决策点 | 选择 | 理由 |
|---|---|---|
| 入栈协议 | HTTP（`/invocations`） | 简单请求-响应，无需双向流式 |
| handler 类型 | 同步 `def` | 转写为阻塞型轮询，线程池执行避免阻塞事件循环 |
| 音频格式 | 仅 WAV | 内置 `wave` 模块零依赖 |
| 输出格式 | 纯文本 | 满足核心场景，可按需扩展 |
| 音频传入 | file_path + audio_url | 兼容平台文件上传与公网 URL 两种来源 |

---

## 三、架构设计

### 3.1 整体架构
```
调用方
  │  HTTPS
  ▼
AgentArts 网关 (gateway_domain)
  │  /runtimes/asr-agent/invocations
  ▼
app.py  ── @app.entrypoint handler(payload, context)
  │
  ▼
service.py  ── transcribe_from_payload(payload)
  │   解析音频来源(file_path / audio_url)
  ▼
iflytek_asr.py  ── XfyunAsrClient.transcribe()
  │   1. upload_audio()  HMAC-SHA1签名 + POST上传  ──> 讯飞 /v2/upload   返回 orderId
  │   2. get_result()    轮询查询                   ──> 讯飞 /v2/getResult 返回 orderResult
  ▼
result_parser.py  ── parse_order_result()
  │   多层嵌套 JSON 解析
  ▼
纯文本  ──> {"response": text, "status": "success"}
```

### 3.2 分层设计原则
采用**业务逻辑与平台接入解耦**的分层结构：
- `app.py` 仅负责 AgentArts SDK 接入（`@app.entrypoint`），不包含业务逻辑
- `service.py` 承载转写业务逻辑，无 SDK 依赖，可被 `app.py` 与 `main.py` 复用
- `iflytek_asr.py` 封装讯飞 API 细节（签名、上传、轮询）
- `result_parser.py` 独立的结果解析，可单测

这样业务逻辑可在本地（`main.py`）与云端（`app.py`）无缝复用，且 SDK 升级不影响业务代码。

---

## 四、项目结构

```
asr_agent/
├── app.py              # AgentArts SDK 入口（@app.entrypoint，入口对象 app）
├── service.py          # 转写服务：payload 解析 + 编排（与 SDK 解耦）
├── iflytek_asr.py      # 讯飞客户端：HMAC-SHA1 签名、上传、轮询查询
├── result_parser.py    # 转写结果解析（多层嵌套 JSON -> 纯文本）
├── config.py           # 环境变量配置（自动加载 .env）
├── main.py             # 本地测试入口（不依赖 SDK，直接转写）
├── requirements.txt    # 依赖（含 pip 镜像加速配置）
├── .env.example        # 密钥与运行参数模板
├── .dockerignore       # 构建镜像时排除敏感文件
├── scripts/
│   ├── init-ecs.sh     # ECS 环境初始化（Python/Docker/SDK/镜像加速）
│   └── deploy.sh       # Mac 一键上传 + 远程部署
├── tests/
│   └── test_local.py   # 解析器单测 + 完整转写测试
├── README.md           # 项目说明
└── DEPLOY.md           # 部署指南
```

---

## 五、核心模块实现

### 5.1 config.py -- 配置管理
基于 `dataclass` 从环境变量读取配置，本地开发自动加载 `.env`（`python-dotenv` 可用时），运行时由平台注入环境变量。

```python
try:
    from dotenv import load_dotenv
    load_dotenv()           # 不覆盖已存在的环境变量（平台注入优先）
except ImportError:
    pass                    # 运行时无 dotenv 也不影响

@dataclass
class Settings:
    app_id: str = field(default_factory=lambda: _env("IFLYTEK_APP_ID"))
    access_key_id: str = field(default_factory=lambda: _env("IFLYTEK_API_KEY"))
    access_key_secret: str = field(default_factory=lambda: _env("IFLYTEK_API_SECRET"))
    language: str = field(default_factory=lambda: _env("IFLYTEK_LANGUAGE", "autodialect"))
    port: int = field(default_factory=lambda: int(_env("AGENT_RUN_PORT", "8080")))
    # ... 轮询参数、临时目录等

    def validate(self) -> None:
        # 缺密钥时抛出明确异常，避免难以排查的网络错误
```

**设计要点**：`load_dotenv()` 默认不覆盖已存在的环境变量，因此平台注入的密钥优先于镜像内 `.env`，保证安全与灵活。

### 5.2 iflytek_asr.py -- 讯飞客户端（核心）

#### 5.2.1 HMAC-SHA1 签名
严格按讯飞文档实现：
1. 排除 `signature` 字段，参数按名自然排序（与 Java TreeMap 一致）
2. 每个 key/value 分别 URL 编码，空值不参与
3. 用 `&` 拼接成 `baseString`
4. HMAC-SHA1（密钥=APISecret）加密后 Base64

```python
def _generate_signature(self, params: dict) -> str:
    sign_params = {k: v for k, v in params.items()
                   if k != "signature" and v is not None and str(v).strip() != ""}
    base_parts = []
    for k in sorted(sign_params.keys()):
        encoded_key = urllib.parse.quote(k, safe="")
        encoded_value = urllib.parse.quote(str(sign_params[k]), safe="")
        base_parts.append(f"{encoded_key}={encoded_value}")
    base_string = "&".join(base_parts)
    hmac_obj = hmac.new(self.access_key_secret.encode("utf-8"),
                        base_string.encode("utf-8"), digestmod="sha1")
    return base64.b64encode(hmac_obj.digest()).decode("utf-8")
```

#### 5.2.2 上传 /v2/upload
拼装参数（appId/accessKeyId/dateTime/signatureRandom/fileSize/fileName/language/duration）-> 签名 -> 以 `application/octet-stream` POST 音频二进制 -> 解析 `orderId`。

WAV 时长用 Python 内置 `wave` 模块计算（无额外依赖）：
```python
with wave.open(file_path, "rb") as w:
    duration_ms = int(round(w.getnframes() / w.getframerate() * 1000))
```

#### 5.2.3 查询 /v2/getResult（轮询）
**关键修正**（相对讯飞官方示例 `Ifasr_llm/Ifasr.py`）：

| 项 | 官方示例 | 本实现 | 依据 |
|---|---|---|---|
| `resultType` | 漏传 | `transfer` | 文档要求必传 |
| `appId`/`ts` | 多传 | 移除 | 文档 getResult 参数表无此两项 |
| `signatureRandom` | 复用 ✓ | 复用 ✓ | 文档要求"与上传接口相同" |
| `dateTime` | 重新生成 ✓ | 重新生成 ✓ | 文档要求"需重新生成" |

轮询逻辑：`status==4` 完成 / `status==-1` 失败 / `failType!=0` 异常，否则按 `poll_interval` 间隔重试，超 `poll_max_wait` 抛超时。

#### 5.2.4 客户端可复用
官方示例在 `__init__` 绑定单个音频文件；本实现将 `transcribe(file_path)` 作为方法，一个客户端实例可转写多个文件，适合服务场景。

### 5.3 result_parser.py -- 结果解析

讯飞返回的 `orderResult` 是**三层嵌套 JSON**（字符串套 JSON 套字符串）：
```
content.orderResult (str) -> {lattice:[{json_1best:"<str>"}]}
json_1best (str)          -> {st:{rt:[{ws:[{cw:[{w:"文字"}]}]}]}}
```

```python
def parse_order_result(api_response: dict) -> str:
    order_result_str = api_response["content"]["orderResult"]
    order_result = _safe_json_loads(order_result_str)      # 第 1 层
    for key in ("lattice", "lattice2"):                     # 优先顺滑结果
        words = _extract_words_from_lattice(order_result.get(key))
        if words:
            return "".join(words)

def _safe_json_loads(text):
    try:
        return json.loads(text)
    except json.JSONDecodeError:
        return json.loads(re.sub(r"\\\\", r"\\", text))     # 兜底：去除多余转义
```

**容错设计**：部分返回存在额外转义（`\\\\`），直接 `json.loads` 失败时用正则降级处理，保证健壮性。

### 5.4 service.py -- 转写服务（业务编排）
将 payload 解析为本地 WAV 文件并完成转写，无 SDK 依赖：

```python
def transcribe_from_payload(payload) -> str:
    settings.validate()
    local_path, is_tmp = _resolve_to_local(payload)   # file_path 或下载 audio_url
    lang, domain = _extract_options(payload)
    try:
        client = XfyunAsrClient(...)
        return client.transcribe(local_path, language=lang, pd=domain)
    finally:
        if is_tmp and os.path.exists(local_path):      # 清理临时文件
            os.unlink(local_path)
```

**payload 多来源兼容**：`file_path`（运行时内文件，经 `ExecuteRuntimeUploadFiles` 上传）/ `audio_url`（公网下载）/ `message`·`prompt`·`input`（字符串路径或 URL）。

### 5.5 app.py -- AgentArts SDK 入口
严格遵循官方 `AgentArtsRuntimeApp` + `@app.entrypoint` 模式：

```python
from agentarts.sdk import AgentArtsRuntimeApp, RequestContext
from service import transcribe_from_payload

app = AgentArtsRuntimeApp()

@app.entrypoint
def handler(payload: dict, context: RequestContext = None) -> dict:
    """同步 def：转写涉及 HTTP 轮询（可能数分钟），
    同步 handler 由 SDK/Starlette 在线程池执行，避免阻塞事件循环。"""
    try:
        text = transcribe_from_payload(payload)
        return {"response": text, "status": "success"}
    except Exception as e:
        return {"response": f"转写失败: {e}", "status": "error"}

if __name__ == "__main__":
    run_port = int(os.getenv("AGENT_RUN_PORT", "8080"))   # 平台端口契约
    app.run(port=run_port)
```

**设计要点**：
- handler 用**同步 `def`** 而非 `async def`：转写是阻塞型 I/O（HTTP 轮询），同步 handler 在线程池执行，不阻塞事件循环；若用 async 会阻塞整个服务
- 入口对象名 `app`，对应 `agentarts configure --entrypoint app:app`
- 端口读 `AGENT_RUN_PORT`（平台契约），非 CLI 参数
- 响应格式 `{"response":..., "status":...}` 符合平台入栈协议约定

---

## 六、数据流程

### 6.1 调用时序
```
调用方                AgentArts网关           app.py            service.py         iflytek_asr.py        讯飞API
  │                      │                     │                  │                    │                   │
  │─POST /invocations───>│                     │                  │                    │                   │
  │  {file_path/audio_url}│                    │                  │                    │                   │
  │                      │─转发到容器8080──────>│                  │                    │                   │
  │                      │                     │─transcribe_from_payload──>│           │                   │
  │                      │                     │                  │─validate+解析音频  │                   │
  │                      │                     │                  │─transcribe()──────>│                   │
  │                      │                     │                  │                    │─签名+POST上传────>│/v2/upload
  │                      │                     │                  │                    │<────orderId───────│
  │                      │                     │                  │                    │─轮询查询(签名)───>│/v2/getResult
  │                      │                     │                  │                    │<──orderResult─────│
  │                      │                     │                  │<──纯文本──────────│                   │
  │                      │                     │<─{response,text}─│                    │                   │
  │<──{response,status}──│<────────────────────│                  │                    │                   │
```

### 6.2 关键状态
- 讯飞订单 `status`：0 已创建 / 3 处理中 / 4 完成 / -1 失败
- `failType`：0 正常 / 1 上传失败 / 2 转码失败 / 3 识别失败 / 4 时长超限 / 5 校验失败 / 6 静音 ...

---

## 七、接口说明

### 7.1 调用接口
`POST https://{gateway_domain}/runtimes/asr-agent/invocations`

请求头：`X-Hw-Agentarts-Session-Id`（必传）、`Authorization: Bearer {API_KEY}`（必传）、`Content-Type: application/json`

请求体（任选一种音频来源）：
```jsonc
{"file_path": "/data/test.wav"}              // 运行时内文件
{"audio_url": "https://.../test.wav"}        // 公网 URL
{"message": "/data/test.wav"}                // message/prompt/input 字段
```

响应：
```json
{"response": "转写文本...", "status": "success"}   // 成功
{"response": "转写失败: ...", "status": "error"}    // 失败
```

### 7.2 辅助接口
- `GET /ping` -> `{"status":"Healthy"}`（SDK 默认健康检查）
- `POST /runtimes/asr-agent/upload-files`（multipart）-> 上传音频到运行时内，配合 `file_path` 使用

---

## 八、部署

### 8.1 部署流程
```
agentarts configure --entrypoint app:app   # 配置（名称/区域/依赖/SWR组织）
agentarts launch                            # 构建 ARM64 镜像 -> 推 SWR -> 创建运行时
```
`agentarts launch` 自动完成 5 步：构建镜像 -> 建 SWR 组织/仓库 -> 获取 SWR 凭证 -> 推送镜像 -> 创建运行时。

### 8.2 运行时配置（控制台）
- **环境变量**：`IFLYTEK_APP_ID` / `IFLYTEK_API_KEY` / `IFLYTEK_API_SECRET`
- **出站网络**：`PUBLIC`
- **访问方式**：自动生成 gateway_domain

### 8.3 部署结果（实测）
```
Agent Name:      asr-agent
Runtime ID:      8d043a4a-c5ff-4576-8f5d-2e3fbd69257a
Access Endpoint: defaultgw-grstqnldg5.cn-southwest-2.huaweicloud-agentarts.com
Region:          cn-southwest-2
```

详细部署步骤见 `DEPLOY.md`。

---

## 九、测试验证

### 9.1 解析器单测（无需密钥/网络）
用讯飞文档示例响应验证 `result_parser`，正确提取 `喂你好`。

### 9.2 本地完整转写（实测通过）
```
python main.py ../Ifasr_llm/audio/lfasr_涉政.wav
```
输出（节选）：
> 中新经纬7月15日电 国家统计局15日公布数据显示……初步核算上半年国内生产总值562,642亿元，按不变价格计算，同比增长2.5%。

验证：签名鉴权 ✓ / 上传 ✓ / 轮询 ✓ / 结果解析 ✓。

### 9.3 云端运行时（已部署）
运行时创建成功，访问域名已生成，待配置环境变量后即可 `curl` 调用验证。

---

## 十、开发与部署中遇到的问题及解决

| # | 问题 | 现象 | 原因 | 解决 |
|---|---|---|---|---|
| 1 | 高代码 PDF 损坏 | pypdf 解析失败 | 文件末尾 `%%EOF` 缺失 | 用户提供修复版后重读 |
| 2 | 讯飞 `000002` | accessKeyId not exist | 录音文件转写大模型服务未开通 | 讯飞控制台开通该服务 |
| 3 | Docker 拉镜像超时 | `python:3.10-slim` i/o timeout | 华为云 Docker 镜像源停服 | 换 `docker.1ms.run` 等可用源 |
| 4 | heredoc 卡住 | shell 一直 `>` 等待 | 粘贴时 `EOF` 被缩进，不顶格 | 改用 `printf` 一行写文件 |
| 5 | 镜像构建 pip 慢 | step8 卡 30+ 分钟 | 容器内 pip 用默认 PyPI | requirements.txt 顶部加 `--index-url` 华为云 PyPI 镜像 |
| 6 | SWR 鉴权 `latin-1` | `‘` 编码失败 | AK 值首字符混入弯引号 | 用直引号重新 `export` AK/SK |
| 7 | 运行时创建 404 | Gateway not found | AgentArts 服务未开通 | 控制台开通 AgentArts 服务 |

---

## 十一、约束与扩展

### 11.1 当前约束
- 仅支持 **WAV**（内置 `wave` 模块算时长，无额外依赖）
- 采样率 16kHz/8kHz、16bit、单声道；时长 ≤5h、文件 ≤500MB
- 输出**纯文本**

### 11.2 可扩展方向
- **多音频格式**：引入 `pydub`/`ffprobe` 支持 mp3/opus/flac 等
- **SRT 字幕**：利用 `bg`/`ed`/`wb`/`we` 时间戳生成带时间轴字幕
- **分角色对话**：利用 `rl` 字段按说话人分段
- **SSE 流式**：`@app.entrypoint` 返回生成器，实现转写进度流式推送
- **MCP 协议**：将运行时改为 MCP Server 模式，作为工具被其他智能体调用
- **AgentArts 组件集成**：接入记忆库（多轮转写上下文）、网关（对接企业内网）

---

## 十二、参考文档

- 讯飞录音文件转写大模型 API：https://www.xfyun.cn/doc/spark/asr_llm/Ifasr_llm.html
- 华为云 AgentArts 高代码开发指南（文档版本 02，2026-08-04）
- 华为云 AgentArts API 参考（文档版本 03，2026-08-03）
- 项目内 `README.md`（使用说明）、`DEPLOY.md`（部署指南）
