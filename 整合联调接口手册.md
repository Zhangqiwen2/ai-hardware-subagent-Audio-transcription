# 录音文件转写 Agent - ConvAIAgent 调用方接口手册

> 供 ConvAIAgent（平台侧调用方）对接本 Agent 使用。覆盖 spec FE2026081400138 的 operation 路由接口。
> 本 Agent **同步异步都支持**：chat_completions（同步阻塞至转写完成）+ create_response/fetch_response（异步）。

---

## 1. 能力声明（query_capabilities）

在调用前，平台可先调本接口确认能力（spec 5.17 能力前置校验）。

**Request**
```http
POST runtimes/{runtime_name}/invocations
Content-Type: application/json
```
```json
{ "operation": "query_capabilities" }
```

**Response `200`**（仅含 `capabilities` 对象，无其他元数据字段）
```json
{
  "capabilities": {
    "chat_completions": true,
    "responses_api": true,
    "responses_get_fetch": true
  }
}
```

**要点**：
- `chat_completions=true`：支持同步对话，调用后**阻塞至转写完成**再返回结果
- `responses_api=true`：支持 `create_response`（异步）
- `responses_get_fetch=true`：支持 `fetch_response`（异步查询）

---

## 2. 同步转写（chat_completions，含无 operation 默认路由）

**Request**
```http
POST runtimes/{runtime_name}/invocations
Content-Type: application/json
```
```json
{
  "operation": "chat_completions",
  "input": { "file_url": "https://obs.../audio.mp3" },
  "model_config": [
    {
      "type": "offline_asr",
      "provider": "openai-compatible",
      "endpoint": "https://convai-router/v2",
      "model_name": "ifasr",
      "auth_token": "Bearer <token>"
    }
  ]
}
```

> 不带 `operation` 字段仅带 `input` 的请求**默认走本路由**（spec 5.18.1 规则3 向后兼容）。

**Response `200`**：OpenAI chat.completion 对象
```json
{
  "id": "chatcmpl-xxxxxxxx-...",
  "object": "chat.completion",
  "choices": [
    {
      "index": 0,
      "message": { "role": "assistant", "content": "转写出来的纯文本..." },
      "finish_reason": "stop"
    }
  ]
}
```

**注意**：
- **同步阻塞**：转写耗时与音频时长正相关（约 0.3~1 倍音频时长），调用方需设置足够超时（建议 ≥ 转写预估时长）。长音频建议改用 create_response/fetch_response 异步方式。
- 转写失败返回 `500 E5001`（`{"error":{"code":"E5001","message":"...","type":"transcribe_failed"}}`）。

---

## 3. 创建异步转写任务（create_response）

**Request**
```http
POST runtimes/{runtime_name}/invocations
Content-Type: application/json
```
```json
{
  "operation": "create_response",
  "input": { "file_url": "https://obs.../audio.mp3" },
  "model_config": [
    {
      "type": "offline_asr",
      "provider": "openai-compatible",
      "endpoint": "https://convai-router/v2",
      "model_name": "ifasr",
      "auth_token": "Bearer <token>"
    }
  ]
}
```

**入参说明**
| 字段 | 必填 | 说明 |
|---|---|---|
| `operation` | 是 | `create_response` |
| `input.file_url` | 是 | 音频文件 URL（可下载）。缺失 → `400 E4001` |
| `model_config[]`（顶层，与 input 平级） | 是 | 模型配置数组，须含 `type=offline_asr` 项；写在 input 内亦可（顶层优先） |
| `model_config[].endpoint` | 是 | ConvAIRouter 网关地址（Agent 用其拼 `/v2/upload`、`/v2/getResult`） |
| `model_config[].auth_token` | 是 | Bearer 认证 token（Agent 原样放入 `Authorization` 头） |
| `model_config[].provider/model_name` | 否 | 预留字段，暂不参与转写逻辑 |

> 标准格式（联调对齐）：`model_config` 置于请求**顶层**（与 `input` 平级）；兼容写在 input 内（顶层优先）；
> `input` 也可直接为 URL 字符串（等价 `{"file_url": "<url>"}`）。

**Response `200`**
```json
{
  "response_id": "resp_xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx",
  "status": "in_progress"
}
```

**注意**：
- **⚠️ Session 亲和（实测确认）**：任务保存在创建请求命中实例的内存中，运行时按 `X-Hw-Agentarts-Session-Id` 将请求路由到实例。**fetch_response 必须与 create_response 使用同一个 Session-Id**，否则路由到其他实例、任务不存在，返回 `404 E4006`。
- `response_id` 仅保存在 Agent **内存**中，**24h 有效**。Runtime 重启会导致进行中/已完成的任务丢失，此后 fetch 返回 `404 E4006`。
- 转写由 Agent 后台线程异步执行，本接口**立即返回**，不会等待转写完成。

---

## 4. 查询转写结果（fetch_response）

**Request**
```http
POST runtimes/{runtime_name}/invocations
Content-Type: application/json
```
```json
{
  "operation": "fetch_response",
  "response_id": "resp_xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
}
```

**Response**

- **已完成 `200`**：完整 OpenAI response 对象
```json
{
  "id": "resp_xxxxxxxx-...",
  "object": "response",
  "status": "completed",
  "output": [
    {
      "type": "message",
      "role": "assistant",
      "content": [
        { "type": "output_text", "text": "转写出来的纯文本..." }
      ]
    }
  ]
}
```
- **进行中 `200`**
```json
{ "id": "resp_xxx", "object": "response", "status": "in_progress" }
```
- **转写失败 `200`**（Agent 自扩展，spec 未定义，联调需对齐）
```json
{ "id": "resp_xxx", "object": "response", "status": "failed",
  "error": { "code": "E5001", "message": "<失败原因>", "type": "transcribe_failed" } }
```

---

## 5. 错误码表

| 错误码 | HTTP | 说明 | 触发场景 |
|---|---|---|---|
| E4001 | 400 | 参数非法 | operation 未知枚举；create_response 缺 input；fetch_response 缺 response_id；请求体非对象 |
| E4007 | 400 | 能力不支持 | operation 对应能力标志位为 false（本 Agent 三项能力均声明支持，正常不触发） |
| E4006 | 404 | 响应不存在或已过期 | response_id 不存在 / 过期(>24h) / 非该租户。三种情况**统一返回**，不泄露存在性 |
| E5001 | 500 | 转写失败 | chat_completions 同步转写异常（异步场景返回 200 + status=failed + error） |

**错误响应格式**
```json
{
  "error": {
    "code": "E4007",
    "message": "本 agent 不支持同步对话（chat_completions），请使用 create_response/fetch_response",
    "type": "capability_not_supported"
  }
}
```

---

## 6. 与 ConvAIAgent 的联调关注点

1. **⚠️ Session 亲和（重要）**：create_response 与 fetch_response 必须携带**同一个** `X-Hw-Agentarts-Session-Id`（运行时按 Session 路由到实例，任务在实例内存中；跨 Session 必然 404 E4006，2026-08-18 实测确认）。ConvAIAgent 侧需为同一转写任务的 create/fetch 保持同一 Session。
2. **`convai_response_state` 落库由 ConvAIAgent 负责**：create_response 后写 in_progress，fetch 完成后更新 completed + output_history（Agent 不直接访问该表）。
2. **failed 状态**（Agent 自扩展）：平台 pre-check 不涉及；但 fetch 到 `status=failed` 时，ConvAIAgent 需决定如何落库、是否透传给客户端。**需联调对齐**。
3. **400/404 真实 HTTP 状态码**：Agent 通过 Starlette `JSONResponse` 返回非 200 状态码；平台侧需能正确识别。
4. **Runtime 重启丢任务**：需确认 ConvAIAgent 接受（返回 404 E4006）或提供补偿。
5. **model_config 注入**：`endpoint`/`auth_token` 由 ConvAIAgent（通过 FE2026080500089 模型注入机制 + FE2026080500414 规范）填充，Agent 只消费。
