# 录音文件转写 Agent - 联调指南

> 本文档指导 ConvAIAgent 团队如何与录音文件转写 Agent 进行接口联调。
> 更新日期：2026-08-21

---

## 一、联调前双方信息交换

### 1.1 我方提供（ConvAIAgent 调我们用）

| 项 | 值 |
|---|---|
| 访问域名（gateway） | `defaultgw-grstqnldg5.cn-southwest-2.huaweicloud-agentarts.com` |
| 运行时名称（runtime name） | `asr-agent` |
| 认证方式 | IAM（V11-HMAC-SHA256 签名） |
| 测试音频 URL | `https://asr-test-wav.obs.cn-southwest-2.myhuaweicloud.com/lfasr_%E6%B6%89%E6%94%BF.wav` |
| 接口文档 | 《Agent接口调用指导.md》《样例报文.md》《接口测试报告.md》 |

### 1.2 对方提供（我们调 ConvAIRouter 用）

| 项 | 说明 |
|---|---|
| `offline_asr_upload` 的 endpoint | ConvAIRouter 上传接口完整 URL |
| `offline_asr_get_result` 的 endpoint | ConvAIRouter 查询接口完整 URL |
| `auth_token` | 调 ConvAIRouter 的 Bearer token |

> ⚠️ **当前状态**：ConvAIRouter 未就绪。因此：
> - **阶段1（直调链路）**：不带 `model_config`，Agent 走直调讯飞（用环境变量密钥），**现在就能联**
> - **阶段2（网关链路）**：带 `model_config`，Agent 走 ConvAIRouter，**等 Router 就绪后联**

---

## 二、接口快速上手

### 2.1 统一请求格式

```http
POST https://defaultgw-grstqnldg5.cn-southwest-2.huaweicloud-agentarts.com/runtimes/asr-agent/invocations
Content-Type: application/json
Authorization: <IAM V11 签名>
X-Hw-Agentarts-Session-Id: {会话ID}
```

> ⚠️ IAM 签名需要 V11-HMAC-SHA256 算法，推荐使用 `agentarts invoke` CLI 或华为云 SDK，不要手动计算签名。

请求体（**所有字段包在 `inputs` 里**）：

```json
{
  "inputs": {
    "operation": "query_capabilities | chat_completions | create_response | fetch_response",
    "file_url": ["音频URL"],           // 数组，取第一个
    "model_config": [ ... ],           // 两条配置（upload + get_result），阶段2才用
    "response_id": "resp_xxx"          // fetch_response 用
  }
}
```

### 2.2 四个 operation 一览

| operation | 模式 | 用途 | 必填字段 |
|---|---|---|---|
| `query_capabilities` | 元操作 | 能力查询（pre-check） | 无 |
| `chat_completions` | 同步阻塞 | 短音频转写 | `file_url` |
| `create_response` | 异步 | 长音频转写（立即返回） | `file_url` |
| `fetch_response` | 异步查询 | 查转写结果（轮询） | `response_id` |

### 2.3 关键约束（联调时最容易踩的坑）

1. **Session 亲和**：`create_response` 和 `fetch_response` 必须用**同一个** `X-Hw-Agentarts-Session-Id`，否则 fetch 返回 404 E4006（已实测验证）
2. **file_url 是数组**：`["https://..."]`，不是字符串
3. **inputs 包装**：所有字段在 `inputs` 里，缺了返回 400 E4001
4. **response_id 24h 有效**：过期返回 404 E4006
5. **同步阻塞时长**：约 0.3~1 倍音频时长，长音频（>5 分钟）务必走异步

---

## 三、联调分阶段计划

### 阶段1：直调链路联调（现在就能做）

**目标**：验证 operation 路由、同步/异步、错误码、Session 亲和。

**前提**：不需要 ConvAIRouter，Agent 内部直调讯飞（环境变量密钥已配好）。

**用例清单**（详细报文见《样例报文.md》）：

| # | 场景 | 预期 |
|---|---|---|
| 1 | query_capabilities | 200，SSE 事件流，responseContent 含三能力 true |
| 2 | chat_completions 同步转写 | 200，SSE 事件流，responseContent = 转写文本（约 45 秒） |
| 3 | create_response 异步创建 | 200，SSE 事件流，responseContent 含 response_id + in_progress |
| 4 | fetch_response 立即查询 | 200，SSE 事件流，responseContent 含 in_progress |
| 5 | fetch_response 50 秒后查询 | 200，SSE 事件流，responseContent = 转写文本 |
| 6 | 不存在的 response_id | 404 E4006 |
| 7 | 缺 inputs 包装 | 400 E4001 |
| 8 | 缺 file_url | 400 E4001 |
| 9 | 缺 response_id | 400 E4001 |
| 10 | 跨 Session 查询 | 404 E4006（验证 Session 亲和） |
| 11 | 未知 operation | 400 E4001 |

> 以上 11 个用例我方已全部实测通过（见《接口测试报告.md》），ConvAIAgent 只需按报告复跑一遍验证。

### 阶段2：网关链路联调（等 ConvAIRouter 就绪）

**目标**：验证 model_config 注入、ConvAIRouter 透传。

**前提**：ConvAIRouter 的 `/v2/upload`、`/v2/getResult` 就绪，能提供真实 endpoint 和 auth_token。

**请求体变化**（相比阶段1，增加 `model_config`）：

```json
{
  "inputs": {
    "operation": "create_response",
    "file_url": ["https://obs.../audio.wav"],
    "model_config": [
      {
        "type": "offline_asr_upload",
        "provider": "openai-compatible",
        "endpoint": "https://{ConvAIRouter IP:port}/v2/upload",
        "model_name": "xunfei_offline_asr",
        "auth_token": "Bearer xxx"
      },
      {
        "type": "offline_asr_get_result",
        "provider": "openai-compatible",
        "endpoint": "https://{ConvAIRouter IP:port}/v2/getResult",
        "model_name": "xunfei_offline_asr",
        "auth_token": "Bearer xxx"
      }
    ]
  }
}
```

**验证点**：
- Agent 不拼接路径，直接用 endpoint 的完整 URL
- Bearer auth_token 正确传递到 ConvAIRouter
- 上传参数（fileName/fileSize/language/duration）正确
- 轮询状态机（status 3→4）正确
- 响应透传正确

### 阶段3：端到端联调

**目标**：麟动科技 → ConvAIAgent → 转写Agent → ConvAIRouter → 讯飞 全链路。

**验证点**：
- ConvAIAgent 的 pre-check（query_capabilities）→ 正常放行
- ConvAIAgent 落库 convai_response_state → create 后写 in_progress，fetch 完成后写 completed
- 麟动科技通过 `POST /v1/responses` + `GET /v1/responses/{id}` 拿到转写结果
- `status:"failed"` 场景（我方扩展）如何处理，需对齐

---

## 四、联调联系人信息

| 角色 | 需要谁 | 提供什么 |
|---|---|---|
| 转写 Agent 方（我方） | - | 接口文档、测试报告、API Key、测试音频 |
| ConvAIAgent 方 | ConvAIAgent 团队 | 联调环境、pre-check 逻辑对接 |
| ConvAIRouter 方 | Router 团队 | endpoint、auth_token、接口规范确认 |

---

## 五、附：curl 快速验证脚本

ConvAIAgent 团队可用以下命令快速验证接口连通性（需配置华为云 AK/SK）：

```bash
# 配置凭证
export HUAWEICLOUD_SDK_AK='你的AK'
export HUAWEICLOUD_SDK_SK='你的SK'

# 1. 能力查询
python3 invoke.py -o query_capabilities -s test-001

# 2. 同步转写（阻塞约 45 秒）
python3 invoke.py -o chat_completions -f "https://asr-test-wav.obs.cn-southwest-2.myhuaweicloud.com/lfasr_%E6%B6%89%E6%94%BF.wav" -s test-001
```

或使用 `agentarts invoke` CLI：

```bash
# 能力查询
agentarts invoke --agent asr-agent --payload '{"inputs":{"operation":"query_capabilities"}}'

# 同步转写
agentarts invoke --agent asr-agent --payload '{"inputs":{"operation":"chat_completions","file_url":["https://..."]}}'
```

---

## 附：关键结论

1. **现在就能联阶段1**（直调链路），无需等 ConvAIRouter
2. **Session 亲和是重要约束**，ConvAIAgent 侧需保证 create/fetch 同 Session
3. **model_config 两条配置**是阶段2的事，等 Router 就绪
4. **响应 24h 过期**，ConvAIAgent 落库时需考虑
5. **运行时重启丢任务**（内存态），fetch 会 404，属正常现象，ConvAIAgent 侧需容错
